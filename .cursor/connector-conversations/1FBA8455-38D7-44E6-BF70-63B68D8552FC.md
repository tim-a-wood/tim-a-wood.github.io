# The double jump still doesn’t work. Do I just press jump twi

Updated: 2026-03-06T03:18:55.000Z

---

### **You**

The double jump still doesn’t work. Do I just press jump twice?

### **Assistant**

The double jump still doesn’t work. Do I just press jump twice?

### **Assistant**

_Thinking:_ The user is asking about double jump functionality in their game - they're saying itstill doesn't work and asking if they just press jump twice. I need to: 1. Read the project plan and project overview first (per workspace rules) 2. Findhow double jump is implemented and what the expected behavior is 3. Fix it so double jump works (likely by pressing jump twice) Let me start by reading the required files and then searching for double jump logic.Theproject plan saysdouble jump was added with `jumpsRemaining`, refilled on land, consumed on mid-air jump. There were several commits fixing it (trigger on new press, Arcade one-frame delay, jumpsRemaining counter, requiring jumpsRemaining === 2 for ground jump). So yes, the intended behavior is: press jump once for first jump, press jump again (second press) while in the air for double jump.Let me read the actual jump logic in index.html to see the current state and why it might still not work.

Checking the jump logic in the game code:Checking the jump logic in the game code:

### **Assistant**

_Thinking:_ I see the logic. Thedouble jump requires `jumpJustPressed`. For keyboard: -`jumpJustPressed = (this.cursors.up && this.cursors.up.justDown) || (this.spaceKey && this.spaceKey.justDown) || (this.inputState.jump && !this.jumpWasDownLastFrame)` So for keyboard you need to use the key's `justDown`- which is true only onthe frame the key was pressed. For touch,we use `inputState.jump && !this.jumpWasDownLastFrame`. One potential bug: on mobile/touch, the "jump" button might set`inputState.jump = true` when you tap. But when do we set `inputState.jump`? We needto see how touch/button updates inputState.If the touch sets jump to true andit stays true for multiple frames, then jumpJustPressed would only be true onthe first frame (when jumpWasDownLastFrame was false). So that should work. Wait - what if the user is on keyboard and pressing Space or Up? Then: - jump =spaceKey.isDown or cursors.up.isDown - jumpJustPressed= spaceKey.justDown or cursors.up.justDown So for double jump to work you need to **release** jump after the first jump and**press again** while in the air.So yes - "press jump twice" means: press once (first jump), release, press again while in the air (second jump). If they're holding jump from the first press, jumpJustPressed won't betrue on the next frame (justDown is only truethe frame you press). So the design is: youhave to tap jump a second time while in the air.Let me check - maybe the issue is that `justDown` isn't firing? Or maybe there's a bug with the order of operations - e.g. we set jumpWasDownLastFrame at the end, but what if on the double-jump framewe're not detecting the second press? Actuallyre-reading again: the usersays "Do I just press jump twice?" - so they're asking for confirmation of the intended input. Sothe answer is: **Yes, press jump once to jump, then press jump again while in the air to double jump.** (Youhave to release and press again; holding doesn't do the second jump.) But they also said "The double jump still doesn't work" - somaybe the implementation is still broken. Let me look for possible bugs: 1.**Touch/button**: When using the on-screen button, does it set `inputState.jump`? And is `jumpJustPressed` computed correctly for touch? We have`(this.inputState.jump && !this.jumpWasDownLastFrame)`. So thefirst frame the button is "down",jump is true andjumpWasDownLastFrame is false, so jumpJustPressed is true. The next frame, jumpWasDownLastFrame gets set to true (because weset it to `jump` at end ofupdate). So thesecond tap: when they tap again, inputState.jump goes true again. But wait - whendo we set inputState.jump? It's set in the button pointerdown. So when yourelease the button, we might set it to false on pointerup. So when you tap the second time, inputState.jump becomes true again,and jumpWasDownLastFrame was false (from the previous frame when you weren't pressing), so jumpJustPressed would be true. Good. 2. **Keyboard**: jumpJustPressed = cursors.up.justDown || spaceKey.justDown. So youhave to press thekey again. If thekey repeat (key held down) fires, justDown might only be true on the first repeat? I need to check - inPhaser, justDown is true only for the frame when the key was first pressed. So holding the key: first frame justDown=true, subsequent frames justDown=false. So to double jump you must release and press again. That's correct. Maybe the bug isthat on theframe we're in the air, we're still considered "on ground" for one frame (Arcade physics)?So: - Frame 1: on ground, press jump → ground branch (jumpsRemaining === 2), apply jump, jumpsRemaining = 1- Frame 2: Arcade might still have blocked.down true. We now have jumpsRemaining === 1. Groundbranch requires jumpsRemaining === 2, so wedon't take it. We're "onGround" so we don't take double-jump brancheither. So wedon't apply any jump. Good - we don't burn the second jump. - Frame 3: Now we're in theair (blocked.down false). User presses jump again. jumpJustPressed = true, !onGround = true, jumpsRemaining (1) > 0. So we should apply double jump. So thelogic seemscorrect. Unless... maybe theissue is that we're not detecting "in the air" properly in some edge case? For example, when moving up quickly, is blocked.down ever true? Let me check - the fix mentioned "treating moving-up as in-air". So maybewe need to consider velocity.y < 0 as "in air" for the purpose of allowing double jump? Let me read more of thefile to see the full movement block and if there's any "moving up = in air" logic.

Reading the full movement and input handling to see how jump and touch are wired:Reading the full movement and input handling to see how jump and touch are wired:

### **Assistant**

_Thinking:_ I see a potential bug. For **touch**: when you tap the jump button, `inputState.jump` is set to trueon pointerdown.So on that frame: - jump = true - jumpWasDownLastFrame wasfalse (previous frame) - So jumpJustPressed = (inputState.jump && !jumpWasDownLastFrame) = true But here's the thing: when you're on the ground andtap jump, wetake the first branch (jump && onGround && jumpsRemaining === 2). We apply thejump and set jumpWasDownLastFrame = jump = true at the end.Now you're inthe air. On **touch**, when do you "pressjump again"? You have to tap the button again. When you tap again, pointerdown fires and inputState.jump = true. Soon that frame: - jump = true - jumpJustPressed = (inputState.jump && !jumpWasDownLastFrame). Was jumpWasDownLastFrame true? On the previous frame (while you were in the air), you weren't touching the button, so jumpwas false, so at the end of that frame weset jumpWasDownLastFrame = false. So when you tap again, jumpJustPressed = true && !false = true. Good. One issue: **touch taps are short**. When you tap the jump buttonthe second time, the pointerdown might only last one frame, and that frame we need to have !onGround. So we need to make sure that when the player taps the jump button while in theair, we get one frame where jumpJustPressed is true and onGround is false. That should work. Another possibility: **Phaser's justDown** for keyboard - maybethe key repeat (when you hold thekey) causesissues? Like, when you press space, you get one justDown. Then you release andpress again- you get another justDown. So that should work. Let methink about **Arcade physics and "in air"**. The condition for double jump is `!onGround`, i.e. `!this.player.body.blocked.down`. Sometimes right afterleaving the ground, for a frame or two, the body might still be "touching"the ground (blocked.down true) dueto overlap or physics step. So we might have a window where the player has already left the ground visually but blocked.down is still true for a frame. Then the next frame blocked.down becomes false. So when would the user press jump the second time? Usually they'd press it after they've left the ground. So we should be in a frame where blocked.down is false. Unless... the window where blocked.down is true is long? Unlikely. Actually, the opposite could be the problem: **we might be considered "on ground" for too long** (one frameafter we leave), so when the user presses jump on thatframe we don't take the double-jump branch (because onGround isstill true). And we don't take the ground branch because jumpsRemaining is already 1. So we don't applyany jump. Then onthe next frame, the user mighthave already released the jump key (because they did a quick tap). So jumpJustPressed was true only on that one frame when we were still "on ground".So we missed the double jump!So the fixcould be: **treat "moving upward" as in-air forthe purpose of double jump**. So the condition for doublejump could be:`(!onGround || this.player.body.velocity.y < 0)` i.e. we're in the air OR we're moving upward (so we've leftthe ground).That way, on the frame where we're moving up but Arcade still saysblocked.down, we can still do the double jump if the user presses.Let me check the git history again - "Fix double jump by treating moving-up as in-air for Arcade one-frame ground delay" - so they tried that. But then they did "Refactor double-jump to use jumpsRemaining counter and refill only when solidlyon ground" and "Fix ground jump to require both jumps (jumpsRemaining === 2)". So the current codedoesn't use"moving up = in air".So maybe we needto add that back: allow double jump when (jumpJustPressed && (/* in air */) && jumpsRemaining >0), where "in air" = !onGround OR velocity.y < 0. Thatway the framewhere we're movingup but blocked.down is still true, we can still trigger the double jump.Let me implement: use "inAir" = !onGround || velocity.y< 0 for the double-jump branch,and keep the ground branch as is (only when onGround && jumpsRemaining === 2). So: - Ground jump: jump && onGround && jumpsRemaining === 2 - Double jump: jumpJustPressed && (!onGround || velocity.y < 0) && jumpsRemaining > 0 But wait - if we use (!onGround || velocity.y < 0), then when we're on ground and velocity.y is 0, we're not "in air". When we're on ground and we just applied jump so velocity.y is negative, we'd be considered "in air" forthe double-jump condition. Soon the same frame we applied thefirst jump, we have velocity.y < 0.So we'd consider that "in air".So the double-jump branchcould run on the same frame as the first jumpif the user is still holding and jumpJustPressed is true! No - on the same frame we already took the first branch (jump && onGround && jumpsRemaining === 2), so we don't evaluate the else if. And after that wedecremented jumpsRemaining to 1. So we're good. On the next frame, we havevelocity.y < 0 andmaybe onGround still true. So inAir = true. If the user presses jump again (jumpJustPressed), we'd take the double-jumpbranch. So we'd apply the second jump even while "on ground" (blocked.down). That's exactly what we want - to not miss the double jump on that awkward frame. Let me also consider: we need tomake sure we don't allow double jump when we'reactually on the ground and not moving. So "inair" should be: we're moving upward (left the ground) OR we'renot blocked down. So (velocity.y < 0 || !onGround). When we're standing on ground, velocity.y>= 0 and onGround = true, so (false || false) = false. Good. When we're in the air falling, velocity.y > 0 and onGround = false, so (false || true) = true. Good. When we're in the air going up, velocity.y < 0, so true. When we're in the one-frame delay (going up butblocked.down still true), velocity.y < 0 so true. Perfect. I'll add inAir = (this.player.body.velocity.y < 0 || !onGround) and use it in the double-jump condition.

Checking how `blocked.down` behaves and whether we should treat moving-up as in-air:Checking how `blocked.down` behaves and whether we should treat moving-up as in-air:

### **Assistant**

_Thinking:_ The issue is likely: **Yes, you press jump twice** (tap once to jump, tap again while inthe air). But the"still doesn't work" suggests that when you do that, the second jump doesn'tfire. A common cause: Phaser Arcade can keep`blocked.down` true for one frame after you leave the ground. So the frame whereyou press jump the second time might still have`onGround === true`, so we don't enter the double-jump branch. And if your second press is a quick tap, `jumpJustPressed` might only be true for that one frame—the same frame we're still "on ground". So we missit. Fix: treat "moving upward" as in-air for the double-jump check, so we accept the second jump even on the frame where Arcade still reports blocked.down.

Confirming: press jump once to leave the ground, then press jump again while in the air(release and press again; holding won't trigger the second jump). Fixing a likely bug: Arcade physics can keep `blocked.down` true for one frame after leavingthe ground, so a quick second tap can occur on that frame and the double-jump branch is skipped. Treating "moving upward" as in-air so the second jump is accepted on that frame.Confirming: press jump once to leave the ground, then press jump again while in the air (release and press again; holding won't trigger the second jump). Fixing a likely bug: Arcade physics can keep `blocked.down` true for one frame after leaving the ground, so a quick second tap can occur on that frame and the double-jump branch is skipped. Treating "moving upward" as in-air so the second jump is accepted on that frame.

### **Assistant**

_Thinking:_ Good. Now to answer the user's question and summarize: 1. **Yes** — you press jump twice: first press to jump, then press again while inthe air to double jump (you have to release and press again; holding doesn't trigger the second jump). 2.I fixed a likely bug: the gamenow treats "in air" as either not on ground OR movingupward. That way, on the frame where Phaserstill reports you ason ground (one-frame delay) but you're already moving up, your second jump press will still register. Let me also add a brief in-game hint so the user knows the control.I could add a small on-screen hintlike "Jump: ↑/Space. Double jump: press again in air." - but theuser didn't ask for that, they just asked if they press jump twiceand why it doesn't work. So I'll leave the code fix and explain. I should check tests per the workspace rule. The change is game logic (doublejump condition).The tests in ./tests/ are for RNG/pit helpers per project_overview. I didn't add a new test file for double-jump since the logic is tightly coupled to Phaser (body, velocity, input). I'll note that in the summary.

Here’s what’sgoing on and what was changed.
**Yes — you press jump twice.**
1. Press jumponce to leave the ground.
2. While you’re in the air, press jump again (release, then press; holding doesn’t trigger the second jump).
**Why it felt like it “stilldoesn’t work”**
Phaser’s Arcade physics can keep `blocked.down` true for one frame after you’ve already left the ground. If you tap jump on that exact frame,the game still thought you were“on ground,” so it didn’t run the double-jump branch and your second press was ignored.
**What was changed**“In air” for the double jump is now:- **not on ground**, or
- **moving upward** (`velocity.y< 0`),
so even on that one frame where you’re moving up but still reportedas on ground, a second jump press is counted as in-air and the double jump runs. Therest of thelogic (ground jump only when `jumpsRemaining === 2`, refill on land, etc.) is unchanged.Try again: jump once, then press jump again while in the air (release then press). It should feel consistent now.
**Tests:** No new tests were added. Existing tests in `./tests/` cover RNG/pit helpers. Double-jump is tied toPhaser’s body/velocity and input; unit-testing it would require extracting the condition and feeding it synthetic state, which wasn’t done for this change.Here’s what’s going on and what was changed.
**Yes — you press jump twice.**
1. Press jump once to leave the ground.
2. While you’re in the air, press jump again (release, then press; holding doesn’t trigger the second jump).
**Why it felt like it “still doesn’t work”**
Phaser’s Arcade physics can keep `blocked.down` true for one frame after you’ve already left the ground. If you tap jump on that exact frame, the game still thought you were “on ground,” so it didn’t run the double-jump branch and your second press was ignored.
**What was changed**
“In air” for the double jump is now:
- **not on ground**, or
- **moving upward** (`velocity.y < 0`),
so even on that one frame where you’re moving up but still reported as on ground, a second jump press is counted as in-air and the double jump runs. The rest of the logic (ground jump only when `jumpsRemaining === 2`, refill on land, etc.) is unchanged.
Try again: jump once, then press jump again while in the air (release then press). It should feel consistent now.
**Tests:** No new tests were added. Existing tests in `./tests/` cover RNG/pit helpers. Double-jump is tied to Phaser’s body/velocity and input; unit-testing it would require extracting the condition and feeding it synthetic state, which wasn’t done for this change.[exit: 0]

