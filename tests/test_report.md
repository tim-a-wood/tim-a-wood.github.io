# Test Report
| ID | Line Item | Description | Result | Notes |
|---|---|---|---|---|
| AT-01 | Fresh Load And Startup | Verify the prototype loads into the playable zone without crashes or blank screens. | Not Run | Browser not exercised; change scope is agent charters only (brainstorm/creative guessing carve-out added to standing directives). |
| AT-02 | Movement, Camera, And World Bounds | Verify left/right movement works and the player/camera stay inside the intended bounded world. | Not Run | Same as AT-01. |
| AT-03 | Ground Jump | Verify the first jump works consistently from solid ground. | Not Run | Same as AT-01. |
| AT-04 | Mid-Air Double Jump | Verify the second jump works in mid-air and creates a clear extra hop. | Not Run | Same as AT-01. |
| AT-05 | Landing Refills Jump State | Verify landing restores the ability to perform another full jump + double jump sequence. | Not Run | Same as AT-01. |
| AT-06 | Touch Controls | Verify on-screen controls behave correctly on touch devices. | Not Run | Same as AT-01. |
| AT-07 | Death, Respawn, And Restart | Verify the life system, respawn behavior, game-over state, and restart control. | Not Run | Same as AT-01. |
| AT-08 | HUD And Feedback | Verify the HUD remains visible and updates during play. | Not Run | Same as AT-01. |
| AT-09 | Key Pickup And Door Unlock | Verify the first progression loop works end to end: collect the key on the high ledge, show it in the HUD inventory, then consume it to unlock the left-side door. | Not Run | Same as AT-01. |
| Unit Test Suite | Overall Unit Tests | `python3 tests/room_environment_system.test.py`, `python3 tests/room_ai_helpfulness_dashboard.test.py`, `node tests/room-wizard-environment-copilot.test.js`, `python3 tests/art_bible_swatches.test.py` | Pass | Room helpfulness lifecycle tests, dashboard aggregation test, and copilot JS regression check all passed on 2026-04-01. After Agent OS product context filter (`os-dashboard.html`, 2026-04-01), re-ran `node --test tests/game-logic.test.js` only; browser context switcher and filtered Issues/Opportunities/Ongoing views not exercised in CI. After `os_contexts` scrub on agent `*-status.json` + `itemVisibleInOsProductContext` in `os-dashboard.html` (2026-04-02), re-ran `node --test tests/game-logic.test.js` only; context-filtered lists not exercised in browser in CI. After Issues/Opportunities/Ongoing **Delete** + `avStatusIdsEqual` id matching (`os-dashboard.html`, 2026-04-02), re-ran `node --test tests/game-logic.test.js` only; delete flow not exercised in browser in CI. `research-status.json` added to supervisor static allowlist so Research rows can persist when the server is used. After `scripts/update_dashboards.sh` + README hardening (2026-04-01), re-ran `node --test tests/game-logic.test.js` only as a quick sanity check; full Python suite not re-run for that doc/shell-only diff. After business-plan PDF + `md_business_report_to_pdf.py` (2026-04-01), re-ran `node --test tests/game-logic.test.js` only; Python suite not re-run (no game logic change). After adding business-plan next steps to agent `*-status.json` for Issues dashboard (2026-04-01), re-ran `node --test tests/game-logic.test.js` only. After Issues dashboard auto-rebaseline on agent/disk drift (`os-dashboard.html`, 2026-04-01), re-ran `node --test tests/game-logic.test.js` only; browser Issues view not exercised in CI. After Issues list sort by `avPriorityScore` first (`os-dashboard.html`, 2026-04-01), re-ran `node --test tests/game-logic.test.js` only. After registering `research` in `DASHBOARDS` + Tech nav (`os-dashboard.html`, 2026-04-01), re-ran `node --test tests/game-logic.test.js` only. After Research agent parity (`AGENTS.research`, charter, plain-English Research view, 2026-04-01), re-ran `node --test tests/game-logic.test.js` only. After Opportunities “Start discussion” parity with Issues (`os-dashboard.html`, `rowKind` on `/api/issue-chat` in `os_dashboard_supervisor.py`, 2026-04-01), re-ran `node --test tests/game-logic.test.js` only; supervisor and dashboard chat not exercised in CI. After Ongoing Tasks dashboard + unified status cache load + `in-progress` scrub on agent `*-status.json` (2026-04-01), re-ran `node --test tests/game-logic.test.js` only; browser Ongoing Tasks view not exercised in CI. After passive income market scan (`research/library/findings/passive-income-market-scan-2026-04-01.md`) plus `research/library/INDEX.md` and `research/dashboard.md` updates (2026-04-01), no product code changed; re-ran `node --test tests/game-logic.test.js` only; full Python suite not re-run. After Research dashboard refresh (`os-dashboard.html`, `research-status.json`, `research/dashboard.md`), executive summary email via `scripts/send_weekly_digest.py` + `artifacts/passive-income-executive-summary-email-2026-04-01.md` (2026-04-01), re-ran `node --test tests/game-logic.test.js` only; acceptance tests not run in browser. After Issues/Opportunities/Ongoing IA UX pass in `os-dashboard.html` (lane chips, scoped context strip, unified Ongoing add flow, optional-field `<details>`, richer empty states, sticky table headers, kind badges), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser tables not exercised in CI. After priority-list row layout cleanup (horizontal action toolbar, header contrast, tighter row padding, slightly wider summary column), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser not exercised in CI. After row delete control switched to trash icon with `aria-label` (`os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser not exercised in CI. After global workbench bar compacted to status chip + Workbench `<details>` menu (`os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser not exercised in CI. After workbench control moved into top header and `db-view-header` restyled as bordered title panels (`os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser not exercised in CI. After collapsible agent/prompt panel (persisted) + left-nav “Agent prompts” roster + full agent picker on every dashboard (`os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser not exercised in CI. After agent picker `<select>`, collapsible dashboards nav (left seam), fixed right-column collapse (`gap: 0` when collapsed), default-right-collapsed + `mv-agent-os-agent-panel-expanded` storage (`os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser not exercised in CI. After seam-handle clipping fix (`overflow: visible` on docks, stacking z-index, `display:none` collapsed panel), simplified one-row header (select-only context, larger title/sub/select), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser not exercised in CI. After compact `.db-view-header` / title / icon / home glyph sizing (`os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser not exercised in CI. After removal of all `.db-view-header` panels (sort badges kept for Issues/Opportunities/Ongoing), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser not exercised in CI. After removing visible `av-sort-badge` chips (sort order moved into lane chip `title` tooltips), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser not exercised in CI. After Opportunities + Ongoing “Run in workflow panel” + `avDispatchActionToAgent` opportunity copy and agent `<select>` wiring (`os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` on 2026-04-02 only; browser not exercised in CI. After Truthfulness and Evidence standing directive propagated to all `agents/*/charter.md` (2026-04-02), re-ran `node --test tests/game-logic.test.js` only; no application code changed. After brainstorm/creative-session guessing carve-out appended to standing directives (2026-04-02), re-ran `node --test tests/game-logic.test.js` only. After art bible v0.2 asset pack, ran `python3 tests/art_bible_swatches.test.py` on 2026-04-02 (Pass; skips if Pillow missing). After `send_weekly_digest.send()` temp-file JSON + curl fix, sent art bible PDF via `scripts/send_markdown_pdf_email.py` (Resend email id logged at send time). Design agent memo: `docs/reports/design-review-ashen-hollow-art-bible-v0.2.md` (art bible v0.2 — approve with conditions). After Strategy assurance-copilot opportunity closure: repaired invalid JSON in `strategy-status.json` (curly-quote corruption on opportunity `str-o-1775106803755`), added `decisions/2026-04-02-assurance-copilot-other-income-gate.md`; ran `python3 -c` json parse on `strategy-status.json` (Pass) and `node --test tests/game-logic.test.js` (Pass) on 2026-04-02. After Agent OS Product column on Issues/Opportunities/Ongoing tables, per-table Product filter, and edit/add product scope (persists `os_contexts` / clears for agent default) in `os-dashboard.html` (2026-04-02), re-ran `node --test tests/game-logic.test.js` only; browser dashboard not exercised in CI. After dashboard table layout pass (wider grid columns, spacing) and Product column de-duplicating the header product context with full-scope tooltip (`os-dashboard.html`, 2026-04-02), re-ran `node --test tests/game-logic.test.js` only; browser not exercised in CI. After Marketing business brand guide pamphlet (`docs/mv-business-brand-guide-pamphlet.html`, `artifacts/MV-Business-Brand-Guide-v0.1-2026-04-02.pdf`, `scripts/html_to_pdf.py`), re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-02. After brand guide v0.2 expansion + `artifacts/MV-Business-Brand-Guide-v0.2-2026-04-02.pdf`, re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-02. After Marketing Tuesday weekly update (`artifacts/marketing-weekly-update-2026-04-02.md`) and `marketing-status.json` (opportunity `marketing-o4` done, `tuesday-update` action, `metrics.tuesday_update`), re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-02. After `update_dashboards.sh` Tuesday branch emailing `artifacts/marketing-weekly-update-YYYY-MM-DD.md` via `send_weekly_digest.py` + README note, re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-02. After digest reliability pass (`PYTHONUNBUFFERED`, safer Resend JSON handling, log line after email step, README troubleshooting), re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-02. After queuing Business Brand Guide v0.2 section 1.3 matrix reviews on Strategy, Design, Engineering, Game Director, Orchestration founder decision, and marketing-status cross-ref (2026-04-02), ran `python3 scripts/validate_status_files.py` on those six files (Pass) and `node --test tests/game-logic.test.js` (Pass). After specialist sign-off: updated `docs/mv-business-brand-guide-pamphlet.html` to v0.2.1 (matrix ☑), added `docs/reports/business-brand-guide-v0.2-stakeholder-reviews-2026-04-02.md`, closed strategy-o7/design-o3/eng-o2/gd-o2, regenerated `artifacts/MV-Business-Brand-Guide-v0.2.1-2026-04-02.pdf`; ran validate + `node --test tests/game-logic.test.js` (Pass). After OS document library: added `scripts/build_os_document_library.py`, `docs/os-document-library.html`, `docs/os-documentLibrary.manifest.json`, `docs/reports/os-document-library-dedupe-notes.md`, `tests/os_document_library.test.py`, Guides &amp; policies dashboard + iframe in `os-dashboard.html`; ran `python3 tests/os_document_library.test.py` and `node --test tests/game-logic.test.js` (Pass). After Guides iframe 404 on supervisor: `scripts/os_dashboard_supervisor.py` now serves allowed read-only paths (`docs/`, `agents/`, etc.) for GET; ran inline resolver assertions (Pass) and `python3 tests/os_document_library.test.py` + `node --test tests/game-logic.test.js` (Pass). After document library list layout: `scripts/build_os_document_library.py` adds Grid/List toggle + list CSS + modified date row, regenerated `docs/os-document-library.html`; ran `python3 tests/os_document_library.test.py` (Pass). After policy archive feature: `scripts/archive_policy_document.py`, `POST /api/archive-document` in `os_dashboard_supervisor.py`, Archive buttons in document library + exclude `docs/archived-policies/` from catalog scan, `tests/archive_policy_document.test.py`; ran `python3 tests/archive_policy_document.test.py`, `python3 tests/os_document_library.test.py`, `node --test tests/game-logic.test.js` (Pass). After Markdown in-browser preview (`GET /view/markdown?path=...` via `scripts/render_markdown_view.py` in `os_dashboard_supervisor.py` and `sprite_workbench_server.py`, library card links for `.md`/`.mdc`, `.mdc` included in catalog scan): ran `python3 tests/os_document_library.test.py` + `node --test tests/game-logic.test.js` (Pass) on 2026-04-02; `/view/markdown` HTTP route not exercised in CI. After wider viewer layout + in-page link rewrite (absolute disk paths, `/repo-relative`, relative to current doc): ran `python3 tests/render_markdown_view.test.py`, `python3 tests/os_document_library.test.py`, `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; `/view/markdown` still not exercised in CI. After fixing `docs/brand-charter.html` body width (was 720px; matched user screenshot) + supervisor links to STYLE_GUIDE / library + single-quote href rewrite in viewer: ran `python3 tests/render_markdown_view.test.py`, `python3 tests/os_document_library.test.py`, `node --test tests/game-logic.test.js` (Pass) on 2026-04-03. After supervisor markdown 404 diagnostics (HTML error page when Accept includes text/html, structured JSON reasons, case-insensitive `path` query key, startup log line, README restart note): ran `python3 tests/render_markdown_view.test.py`, `node --test tests/game-logic.test.js` (Pass) on 2026-04-03. After PyPI `markdown` for real HTML preview: `start_agent_os_dashboard.sh` installs `requirements-agent-os.txt` when missing; supervisor stderr reports rendering mode; `_md_to_fragment` distinguishes ImportError vs render failure; ran `python3 tests/render_markdown_view.test.py`, `node --test tests/game-logic.test.js` (Pass) on 2026-04-03. After document library reorganization (tiered categories, exclusions for noise HTML/Cursor exports/checkpoints/artifacts, formal titles, sticky section nav + list-first dense layout): ran `python3 scripts/build_os_document_library.py`, `python3 tests/os_document_library.test.py`, `python3 tests/archive_policy_document.test.py`, `node --test tests/game-logic.test.js` (Pass) on 2026-04-03. After Markdown preview images: `_rewrite_markdown_img_srcs` in `scripts/render_markdown_view.py`, readonly GET for `READONLY_IMAGE_SUFFIXES` in `scripts/os_dashboard_supervisor.py`, `.md-body img` styles; ran `python3 tests/render_markdown_view.test.py` (11 tests, Pass) and `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; supervisor image GET not exercised over HTTP in CI. After static IA mockup `docs/mockups/agent-os-dashboard-simplified-mockup.html` (2026-04-03), no runtime dashboard code changed; test suites not re-run. Mockup updated 2026-04-03: dropped Orchestration/Strategy nav (leadership via tables + specialist picker). After API Usage dashboard (`api-usage` view, nav, charts, mirrored analytics KPIs, `os-dashboard.html` 2026-04-03), re-ran `node --test tests/game-logic.test.js` (Pass); browser dashboard not exercised in CI. After integrating API usage screen + nav swap script into `docs/mockups/agent-os-dashboard-simplified-mockup.html` (2026-04-03), re-ran `node --test tests/game-logic.test.js` (Pass); mockup not exercised in CI. After plain-English copy pass on `os-dashboard.html` (home, API usage, analytics call log, workbench hints, tier labels; follow-up analytics funnel footnote), re-ran `node --test tests/game-logic.test.js` (Pass); browser not exercised in CI. After mockup `docs/mockups/agent-os-dashboard-simplified-mockup.html` plain-English alignment with live API usage copy + sidebar `aria-label` tweak, re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; mockup not exercised in CI. After API usage dashboard simplification (`usage_charts.cost_rollups` in `scripts/workbench_persistence.py`, provider cost table + UTC date range + daily stacked spend chart in `os-dashboard.html`), ran `python3 -m pytest tests/test_sprite_workbench.py::SpriteWorkbenchTests::test_usage_ledger_charts_rollup tests/test_sprite_workbench.py::SpriteWorkbenchTests::test_usage_cost_rollups_groups_by_day_and_provider` and `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; browser API usage view not exercised in CI. After mockup `docs/mockups/agent-os-dashboard-simplified-mockup.html` updated to match simplified API usage IA (connection row, range + table + single chart placeholder), re-ran `node --test tests/game-logic.test.js` (Pass); mock HTML not exercised in CI. After adding `openai_key_set` (`OPENAI_API_KEY`) to dashboard payload + UI (Home/API usage chip, dev/anim/ld/cybersecurity cards), OpenAI row in provider mix rollup, and `tests/test_workbench_local_control.py`, ran `python3 -m pytest tests/test_workbench_local_control.py tests/test_sprite_workbench.py::SpriteWorkbenchTests::test_usage_ledger_charts_rollup` and `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; browser not exercised in CI. After porting mock IA to `os-dashboard.html` (Work nav label, collapsible All role dashboards, agent list opens matching dashboard, API usage lede, header subtitle), re-ran `node --test tests/game-logic.test.js` (Pass); browser nav not exercised in CI. Reverted collapsible nav (restored always-visible Core / Leadership / Tech / Business / Game / Product groups) and agent-only panel focus; re-ran `node --test tests/game-logic.test.js` (Pass); browser not exercised in CI. Removed Leadership/Tech/Business/Game from live left nav (Work + Product only), restored All agents → `selectDashboard` via `AGENT_ID_TO_DASHBOARD`; updated mock banner; re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; browser nav not exercised in CI. Removed left-rail Agent prompts section (toggle + All agents); agent UX is right panel only; dropped `AGENT_ID_TO_DASHBOARD` / nav-agent listeners; mock sidebar aligned; re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-03. After supervisor API usage ledger path fix (`scripts/workbench_local_control._usage_ledger_json_path` → Sprite Workbench `projects-data/_usage_ledger.json`), ran `python3 tests/test_workbench_local_control.py` and `node --test tests/game-logic.test.js` (Pass) on 2026-04-03. After usage rollups counting all ledger rows (not only priced) + API usage UI call-only mode (`workbench_persistence.py`, `os-dashboard.html`), ran `python3 -m pytest tests/test_sprite_workbench.py::SpriteWorkbenchTests::test_usage_cost_rollups_groups_by_day_and_provider tests/test_sprite_workbench.py::SpriteWorkbenchTests::test_usage_ledger_charts_rollup`, `python3 tests/test_workbench_local_control.py`, and `node --test tests/game-logic.test.js` (Pass) on 2026-04-03. After removing the Home dashboard from `os-dashboard.html` (default landing → API usage), ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; Agent OS HTML not exercised in CI. After Agents home left rail + specialist modal + `POST /api/agent-chat` (`os-dashboard.html`, `scripts/os_dashboard_supervisor.py`, mockup `docs/mockups/agent-os-agents-home-mockup.html`, Design charter mockup-first directive): ran `python3 tests/os_dashboard_agent_chat.test.py` and `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; HTTP `/api/agent-chat` and browser Agent OS not exercised in CI. After Home moved to a main-column dashboard with a **Start → Home** nav link only (tiles removed from left rail; `DASHBOARDS.home`, mockup/handoff updated): ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; browser not exercised in CI. After Agent OS dark control surfaces (`--os-control` / `--os-sunken` replacing white-tint rgba fills) + larger categorized Home tiles: ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; browser not exercised in CI. After centering issue-chat and agent-hub modals in the viewport (`os-dashboard.html`): ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; browser not exercised in CI. After Agent OS button contrast pass (`--os-control-border`, filled `.av-btn`, agent cards, issue-chat and global workbench controls) in `os-dashboard.html` (2026-04-03), re-ran `node --test tests/game-logic.test.js` (Pass); browser not exercised in CI. After agent hub footer quick actions (duplicate agent-specific buttons above chat input via `agentHubPopulateActionButtons`), re-ran `node --test tests/game-logic.test.js` and `python3 tests/os_dashboard_agent_chat.test.py` (Pass); browser modal not exercised in CI. After agent hub **Charter actions** row (charter `## Actions` via `*-status.json`, task-context insert + Shift+open output), re-ran `node --test tests/game-logic.test.js` and `python3 tests/os_dashboard_agent_chat.test.py` (Pass); browser not exercised in CI. After fixing block-comment `*/` terminator inside `agents/*/charter` JSDoc (script parse failure broke entire Agent OS), re-ran `node --check` on extracted `os-dashboard.html` script (Pass) and `node --test tests/game-logic.test.js` (Pass). After moving agent hub Charter actions above the chat scroll (removed duplicate strip above composer), re-ran `node --test tests/game-logic.test.js` (Pass); browser not exercised in CI. After agent hub / issue chat modal flex fix (`min-height: 0` on scroll, `flex-shrink: 0` on chrome, wider panels, taller `max-height`, task textarea `max-height`), re-ran `node --test tests/game-logic.test.js` (Pass); browser not exercised in CI. After sprite-arch HI-FI mockup Ashen nav dot uses `--c-ashen-hollow` per STYLE_GUIDE §11 (`docs/mockups/agent-os-sprite-arch-dashboard-mockup.html`), re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; mockup not exercised in CI. After M2 `sprite-arch` dashboard shell (`os-dashboard.html`, stub `artifacts/sprite-workbench-arch/manifest.json`, supervisor static allowlist), re-ran `node --test tests/game-logic.test.js` (Pass) and `python3 -c` JSON parse on stub manifest (Pass) on 2026-04-03; browser `sprite-arch` view and supervisor GET not exercised in CI. After P0 extractor + Structure list (`scripts/extract_sprite_workbench_arch.py`, committed manifest, `tests/extract_sprite_workbench_arch.test.py`, CI workflow step), re-ran `node --test tests/game-logic.test.js` and `python3 tests/extract_sprite_workbench_arch.test.py` (Pass) on 2026-04-03; browser Structure tab not exercised in CI. After `/api/sprite-workbench-arch-manifest` on supervisor + workbench + dashboard fetch fallback, re-ran extractor, `python3 tests/extract_sprite_workbench_arch.test.py`, and `node --test tests/game-logic.test.js` (Pass); HTTP route not exercised in CI. After sprite-arch vis-network graph (jsDelivr), relationship edge filters, change heat, inspector exports, `POST /api/sprite-arch-explain` (supervisor + workbench lazy import), manifest verify ignoring git-derived churn, CI verify step, and extractor test bump to schema 0.2 (`os-dashboard.html`, `scripts/os_dashboard_supervisor.py`, `scripts/sprite_workbench_server.py`, `scripts/verify_sprite_workbench_arch_manifest.py`, `tests/extract_sprite_workbench_arch.test.py`, `.github/workflows/test.yml`, 2026-04-03), ran `python3 tests/extract_sprite_workbench_arch.test.py`, `python3 scripts/verify_sprite_workbench_arch_manifest.py`, and `node --test tests/game-logic.test.js` (Pass); browser sprite-arch UI and explain HTTP not exercised in CI. After sprite-arch layout: taller graph viewport (`min(72vh, 800px)` / `min-height: 520px`) and inspector as floating overlay on the graph (`os-dashboard.html`, 2026-04-03), re-ran `node --test tests/game-logic.test.js` (Pass); browser layout not exercised in CI. After sprite-arch view differentiation (Structure = folder two-line labels + load-order edges only + folder bucket colors; Relationship = all edges with kind labels + JS box vs Python ellipse; Change = no edges + churn sizing/labels + dim zero-churn; `os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` (Pass); three-tab graphs not exercised in browser in CI. After sprite-arch readability pass (taller graph canvas, larger fonts, node width/height constraints, thicker edges, continuous edges, stronger barnesHut spacing + avoidOverlap, min zoom floor after fit, nav zoom buttons; `os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` (Pass); graph not re-checked in browser in CI. After sprite-arch layout pass v2 (collapsible overview/KPI drawer default closed, graph flex-fills column + ResizeObserver, JS/Python column seeds for relationship/change, higher min zoom, edge text labels removed, tuned physics; `os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` (Pass); graph not re-checked in browser in CI. After sprite-arch layout without zoom floor: Relationship + Change use fixed grids (physics off); Structure stays hierarchical; fit() only for framing; ResizeObserver refits on resize (`os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` (Pass). After sprite-arch grid spacing fix (cell step wider than node max width, taller rows, 3 cols max for relationship, wider JS/Python column centers; `os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` (Pass). After sprite-arch **Change** tab switched from vis-network to DOM churn heat map (`spriteArchRenderChangeHeatmap`, `spriteArchRefreshView` branch, edge filters skip Change), re-ran `node --test tests/game-logic.test.js` and `python3 tests/extract_sprite_workbench_arch.test.py` (Pass) on 2026-04-03; browser Change heat map not exercised in CI. After sprite-arch heat map **gradient** pass (muted → warning → error blends, smoothstep tile ramp, legend multi-stop), re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; browser not exercised in CI. After issue-chat modal sizing (taller/wider panel, min-height message column, scrollable API hint + thinking block); re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; modal not exercised in browser in CI. After `docs/pixel-art-quality-standards.md` + `animation-status.json` priority 3 closed (2026-04-03), ran `python3 scripts/validate_status_files.py animation-status.json` and `node --test tests/game-logic.test.js` (Pass); acceptance tests not run; change is documentation and status JSON only. After publishing pixel standards to OS document library: `build_os_document_library.py` categorizes `docs/pixel-art-quality-standards.md` under sprite_tooling + formal title; ran `python3 tests/os_document_library.test.py` and `node --test tests/game-logic.test.js` (Pass). After standing directives on mandatory use of pixel standards: `agents/animation/charter.md`, `agents/level-design/charter.md`, `agents/creative/charter.md` (links `../../docs/pixel-art-quality-standards.md`); re-ran `node --test tests/game-logic.test.js` (Pass). After extending same policy to Game Director, Engineering, Orchestrator charters + `AGENTS.md` OS section; re-ran `node --test tests/game-logic.test.js` (Pass).  After Home dashboard API usage summary (`home-kpi-*`, `syncHomeApiUsageSummary`, session restore `home`), re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-03; Agent OS Home view not exercised in browser in CI. After API usage empty-state fix (`__dashboardHasPayload`, `apiUsageEmptyCopy`, `cost_rollups.ledger_row_count` / `dated_row_count`), ran `pytest` usage rollup tests + `node --test tests/game-logic.test.js` (Pass) on 2026-04-03. After `docs/my-actions-integration-plan.md` (Agent OS My Actions engineering handoff) and mockup nav/header count sync in `my-actions-mockup.html` (2026-04-04), re-ran `node --test tests/game-logic.test.js` (Pass); My Actions view not yet wired into `os-dashboard.html`. After agent filter `<select>` + `data-agent` on mockup cards (2026-04-04), re-ran `node --test tests/game-logic.test.js` (Pass); mockup not exercised in browser in CI. After aligning mockup agent ids with `AGENTS` in `os-dashboard.html` (`orchestrator`, Development/Audio Director labels, 2026-04-04), re-ran `node --test tests/game-logic.test.js` (Pass). After My Actions kanban integrated into `os-dashboard.html` (nav, view, `*-status.json` aggregate, `localStorage` `mv-agent-os-my-actions-v1`, product-context filter), added `tests/my_actions_aggregate.test.js`; ran `node --test tests/game-logic.test.js tests/my_actions_aggregate.test.js` (Pass) on 2026-04-04; browser DnD not exercised in CI. After My Actions card click → centered detail overlay (`note` / `proposed_solution` / risk from status JSON, plain-English section labels), re-ran `node --test tests/game-logic.test.js tests/my_actions_aggregate.test.js` (Pass) on 2026-04-04; overlay not exercised in browser in CI. After richer overlay copy + product scope + optional summary/owner/stakeholders + deduped file links (status JSON, charter, orchestration synthesis sources, paths scraped from notes), re-ran `node --test tests/game-logic.test.js tests/my_actions_aggregate.test.js` (Pass) on 2026-04-04. After widening My Actions detail overlay (`ma-detail-panel` ~800px / 96vw), re-ran `node --test tests/game-logic.test.js tests/my_actions_aggregate.test.js` (Pass) on 2026-04-04. After My Actions detail OpenAI discussion (`POST /api/my-actions-chat`, in-panel chat + Apply for review rows, `tests/os_dashboard_my_actions_chat.test.py`), ran `python3 tests/os_dashboard_my_actions_chat.test.py` and `node --test tests/game-logic.test.js tests/my_actions_aggregate.test.js` (Pass) on 2026-04-04; browser overlay and live OpenAI call not exercised in CI. After My Actions detail two-column layout (ticket left, OpenAI discussion right from 768px; stacked on narrow viewports), re-ran `node --test tests/game-logic.test.js tests/my_actions_aggregate.test.js` (Pass) on 2026-04-04; browser layout not re-checked in CI. After coding-agent mockup-first alignment (`CLAUDE.md`, `AGENTS.md`, `.cursor/rules/frontend-design.mdc`), re-ran `node --test tests/game-logic.test.js tests/my_actions_aggregate.test.js` (Pass) on 2026-04-04. After universal **My Actions** post-request check directive (`AGENTS.md`, `CLAUDE.md`, orchestrator charter), re-ran `node --test tests/game-logic.test.js tests/my_actions_aggregate.test.js` (Pass) on 2026-04-04. After clarifying My Actions directive (add/remove/edit rows owned in own `*-status.json` only), re-ran `node --test tests/game-logic.test.js tests/my_actions_aggregate.test.js` (Pass) on 2026-04-04. After API usage personal-cache lede + empty-state hints + `MergedUsageLedgerTests` (`os-dashboard.html`, `tests/test_workbench_local_control.py`), re-ran `python3 tests/test_workbench_local_control.py` and `node --test tests/game-logic.test.js` (Pass) on 2026-04-04. After API usage log-total KPIs + UTC range inclusive end + auto “All time” when narrow range is empty (`os-dashboard.html`), re-ran `node --test tests/game-logic.test.js` (Pass) on 2026-04-04. After OpenAI pull script (costs + completions usage fallback, env autoload, `rollup_call_count` in `workbench_persistence.py`) + `test_usage_cost_rollups_respects_rollup_call_count`, ran `pytest` on those two rollup tests (Pass) on 2026-04-04. After OpenAI token USD estimate for completions usage, `POST /api/pull-openai-personal-usage`, dashboard “Pull OpenAI usage” button, and tests (`test_pull_openai_organization_costs_cache.py`, `run_pull_openai_personal_usage_cache`), ran `pytest` on pull + workbench + rollup tests and `node --test tests/game-logic.test.js` (Pass) on 2026-04-04. After room editor Environment Describe/Review IA, split review column, plain-English build labels, collapsed checklist by default (`room-layout-editor.html`, 2026-04-05): `node tests/room-wizard-results-contract.test.js` and `node tests/game-logic.test.js` (Pass); browser and calibration screenshots not re-run. After debug preview reference upload + `debug_preview_reference_images` payload (`room-layout-editor.html`, `scripts/room_environment_system.py`, 2026-04-05): `python3 tests/room_environment_system.test.py RoomEnvironmentSystemTests.test_coerce_debug_preview_reference_images` (Pass); full `room_environment_system` suite not re-run. After Environment wizard copy/IA pass (numbered steps, Generate preview pictures label, Go to Describe, collapsed project kit; `room-layout-editor.html`, 2026-04-05): `node tests/room-wizard-results-contract.test.js` and `node tests/game-logic.test.js` (Pass); browser flow not exercised in CI. After CI workflow change (2026-04-06): added `python3 tests/room_environment_system.test.py` to `.github/workflows/test.yml`; full suite re-run locally (115 tests, ~50s, exit 0). After per-biome preview frozen concepts (2026-04-06): `generate_room_environment_previews` uses the biome pack that matches `spec.theme_id` and prefers that pack's `locked_concept_ids` for Gemini preview refs (fallback: project `frozen_concept_ids`); `preview.scene_plan` adds `preview_frozen_concept_ids` and `preview_biome_id`. Ran `python3 tests/room_environment_system.test.py` (117 tests, ~53s, Pass), `node tests/room-wizard-environment.test.js`, `node tests/room-wizard-results-contract.test.js` (Pass). Removed debug preview reference uploads (editor + `debug_preview_reference_images` API); preview uses frozen concepts only (2026-04-06). Re-ran `python3 tests/room_environment_system.test.py` (116 tests, ~54s, Pass), `node tests/room-wizard-environment.test.js`, `node tests/room-wizard-results-contract.test.js`, `node tests/room-wizard-environment-copilot.test.js`, `node tests/game-logic.test.js` (Pass). Map MVP automation (2026-04-06): `tests/game-logic.test.js` adds `resolveFinalGateState` + `simulateSequenceAttempt` parity tests vs `index.html`; added `docs/pacing-tuning-v1.md` skeleton; updated `prompts/project_plan.md` Activity 5/8 checkboxes. Re-ran `node tests/game-logic.test.js` (Pass). Environment Results nested-panel reduction (2026-04-05): Results column skips the large generated preview card when gallery thumbnails exist; `#roomWizardCopilotPreview` uses `rw-copilot-preview-box--flush` inside the stage card; build summary top block uses `rw-environment-stage-hero` (no inner bordered card). Ran `node tests/room-wizard-results-contract.test.js`, `node tests/room-wizard-environment-copilot.test.js`, `node tests/room-wizard-environment.test.js`, `node tests/game-logic.test.js` (Pass); browser not exercised. Build summary column (2026-04-05): generated image grid moved directly under the title/status row; dense metadata + seed/reference chips live in a collapsed **Build details** `<details>`; review column `max-height` relaxed to `min(82vh, 880px)`. Ran `node tests/room-wizard-results-contract.test.js`, `node tests/room-wizard-environment-copilot.test.js`, `node tests/game-logic.test.js` (Pass); browser not exercised. Ruins playtest void (`index.html`): camera clear `#101820` and starfield tint `0x7a8fa3` replace warm brown/gold so editor embed and playtest sky band are not sepia-washed; `node tests/game-logic.test.js` (Pass); browser not re-checked. Bespoke playtest shell (`index.html`): higher alpha on procedural wall/floor physics tiles when `emphasizeWalls`; bespoke wall modules + AI wall mass at depth 0.14–0.17 above floor cap; single stretched primary floor cap when bespoke `primary_floor_piece` exists (no band/face stack); `node tests/game-logic.test.js` (Pass); browser not re-checked. Wall shell: room-local X + `wall_piece` fallback via `getRoomEnvironmentBespokeWallShellAssets`; primary floor **face** decor band uses `primaryFloorLocalLeft` (not doubled world X). New smoke test `testIndexBespokeWallShellPlacementContract`. Playtest wall gate: `roomHasPolygonWallTileRects` (full-room polygon → empty rects still runs bespoke wall shell); `addRoomBespokeWallShellDecor` returns false when wall slots exist but nothing placed (outer mass fallback). Wall mass uses procedural `env-surface-wall` when no AI `wall_body_strip`; tile scale from frame size (`applyWallStripTileScaleFromTexture`); `emphasizeWalls` when room has footprint polygon. Bespoke wall shell: authored width/height + clamp; bottom `origin_y: 1` (#151); no `Math.max(..., full chamberH)` vertical stretch. Width policy #150. Bespoke shell always runs; polygon rect gate only on mass fallback (#148). Wall shell #152: cap display to loaded texture frame; skip in-shell flanking `addRoomWallMassDecor` when `wall_module_*` assets exist (mass fallback still in `buildWorldGeometry` if bespoke fails). #153: narrow width — `capW` 14% chamber max 272px, `WALL_ART_MAX_SCALE_W` 1 (no horizontal upscale), fallback branch clamped. `node --test tests/game-logic.test.js` (Pass) on 2026-04-05; browser not re-checked.|
| RW-1 manual | Room wizard vertical slice | Add Room → Layout → Review → Export JSON (per `docs/room-wizard-implementation-sprints.md`). | Not Run | Automated unit tests only; browser demo not executed in CI. |
| RW-2 manual | Neighbors & alignment | Adjoining room → Align → Match opening height; global map shows link (per `docs/room-wizard-implementation-sprints.md` §RW-2). | Not Run | Automated unit tests only; browser not exercised in CI. |
| RW-AI-01 manual | Room AI helpfulness smoke | Project-backed room flow: build environment preview, record preview view, approve suggestion, save later edit, then confirm `/api/dashboard-data` exposes helpfulness rollups. | Pass | Live HTTP smoke pass against `sprite_workbench_server.py` on 2026-04-01 succeeded after fixing two regressions found during QA: `suggestion_id` hashing needed string coercion, and dashboard scanning needed `room_layout.json` rather than `room-layout.json`. |

Update 2026-04-06: wall shell boundary-flush anchor fix landed (`index.html`): left shell now uses right-edge anchor (`origin_x: 1`) at `chamberBounds.left`; right shell anchors at `chamberBounds.right`. Primary floor seam fix also landed: cap/face decor now spans full chamber bounds and uses `primaryFloorY = chamberBottom` so floor top is flush with boundary and meets both side walls. Follow-up physics alignment fix: primary-floor collision tiles now also use chamber bounds + `chamberBottom` (instead of authored platform `x/y/len`) so player feet are flush with the visible floor seam. Final gate fix: `isPrimaryFloor` no longer requires legacy `y` equality, so chamber-bottom collision branch is not skipped. Verified via `node --test tests/game-logic.test.js` (Pass). Manual browser acceptance tests were not rerun in CI.

Update 2026-04-06 (later): walk plane fix #158 — floor seam, collision, and wall `wallFootY` use `getPrimaryFloorTileCenterY` / `getRoomWalkPlaneTopY` so polygon bottom cannot sit below the primary platform row and desync art vs physics. `node --test tests/game-logic.test.js` (Pass); browser not re-checked in CI.

Update 2026-04-06 (later): bespoke wall shell **#159** — when `wall_module_*` assets are present, flanking mass is skipped; side shell display width is now at least the room→chamber inset (`marginLeft` / `marginRight`) so a narrow cap (e.g. 272px) cannot leave a black strip at the room edge. `node --test tests/game-logic.test.js` (Pass); manual playtest not re-run in CI.

Update 2026-04-06 (later): floor line **#160** — primary floor physics, wall foot, and cap use `getLayoutFloorTileCenterY` (`polygonBounds.bottom - 16` when a footprint exists); floor cap image uses top origin at `walkTop` so the cap top matches the walk surface. `node --test tests/game-logic.test.js` (Pass); browser not re-run in CI.

Update 2026-04-06 (later): Gemini env + diagnostics — `load_repo_env_local()` now fills vars from `.env.local` when the shell value is empty; `_gemini_api_key()` accepts `GOOGLE_API_KEY`; `_gemini_generate_content_rest` logs HTTP/API errors (no secrets). Ran `python3 tests/room_environment_system.test.py` (116 tests, Pass). Browser generation not re-run in CI.

Update 2026-04-06 (later): **#161b** — `.env.local` non-empty lines always override the shell; `room_layout_copilot` aligned; `PIXELLAB_API_KEY` assigned after `load_repo_env_local()`; copilot accepts `GOOGLE_API_KEY`. Ran `python3 tests/room_environment_system.test.py` (Pass), `pytest tests/test_workbench_local_control.py` (Pass).

Update 2026-04-06 (later): Gemini bespoke generation — `_gemini_generate_content_rest` / `_generate_image_from_references` / `_generate_bespoke_component_from_references` return `(success, user_safe_error)`; bespoke build records `gemini_error` on failed attempts; `component_type` passed through the bespoke path; `_generate_level3_image_with_gemini` initializes `response`/`gem_err` before try/except. Unit tests updated mocks to return `(True, None)`. Ran `python3 tests/room_environment_system.test.py` (116 tests, Pass). Browser Gemini build not re-run in CI.

Update 2026-04-06 (later): Room env MVP diagnostics — `lastGeminiImageError` snapshot + `geminiImageModel` / `geminiTextModel` on `GET /api/ping`; optional `?probe=1` runs `gemini_image_probe()` (one image API call). Editor shows image model + last error, **Test image API** button, build summary shows `gemini_error` on failed slots and clearer copy when `slot_generation_failed`. Ran `python3 tests/room_environment_system.test.py` (117 tests, Pass); browser not exercised in CI.

Update 2026-04-06 (later): Editor Copilot meta line adds a **Billing** hint when the last Gemini error looks like quota/spending cap (429, etc.). `.env.local.example` notes fixing Google Cloud spending cap for 429. No automated test (copy-only).

Update 2026-04-06 (later): Playtest camera — `getRoomCameraChamberBoundsWorld(roomId)` clamps `cameras.main.setBounds` to the layout **footprint polygon** plus bleed when present: vertical `CAMERA_CHAMBER_SURFACE_BLEED_PX` (32px), horizontal `CAMERA_CHAMBER_SIDE_BLEED_PX` (64px) for wall crop; no polygon → full room slot. Runtime review capture uses the same rect. Ran `node --test tests/game-logic.test.js` (Pass); in-browser framing not re-checked in CI.

Update 2026-04-06 (later): `CAMERA_CHAMBER_SIDE_BLEED_PX` raised 48 → 64 (founder playtest still tight on side walls). Ran `node --test tests/game-logic.test.js` (Pass).

Update 2026-04-06 (later): Primary floor cap, face band, and primary-floor **collision** tiles extend horizontally by `CAMERA_CHAMBER_SIDE_BLEED_PX` (fallback: surface bleed) past polygon left/right (clamped to room width) so wall–floor corners match camera horizontal bleed. Ran `node --test tests/game-logic.test.js` (Pass); browser not re-checked.

Update 2026-04-06: Room env v3 — optional omission of `backwall_panel` planner slots via `MV_V3_BACKWALL_PANEL=0` (`scripts/room_environment_v3.py`); documented in `.env.local.example`. Added `test_v3_planner_omits_backwall_panel_when_env_disabled`. Ran `python3 tests/room_environment_system.test.py` (full suite, Pass).

Update 2026-04-06: Room editor Environment **Step 2 — Preview & build** UX: optional `<details>` for revise fields + **Update preview**; primary gold CTA for **Build final room assets**; hint copy for build vs update; **Discard previews** separated. Ran `node tests/room-wizard-results-contract.test.js`, `node tests/room-wizard-environment.test.js`, `node tests/room-wizard-environment-copilot.test.js` (Pass); browser not re-checked in CI.

Update 2026-04-06: Preview gallery cards — **click image opens full size** (`target="_blank"` link); title/level grid + `minmax(0,1fr)` + nowrap approve label for consistent wrapping (`room-wizard-workbench-shell.css`). Ran `node tests/room-wizard-results-contract.test.js`, `node tests/room-wizard-environment.test.js` (Pass).

Update 2026-04-07: Preview open link: `cursor: pointer` + `text-decoration: none` on `.rw-preview-card-open`. Ran `node tests/room-wizard-results-contract.test.js` (Pass).

Update 2026-04-06: Full-size preview opens `room-environment-preview-full.html` (dark full-viewport page, `src` query same-origin only) instead of linking directly to the PNG (avoids browser default white image margins). Ran `node tests/room-wizard-results-contract.test.js` (Pass); browser not re-checked in CI.

Update 2026-04-06: Environment **Preview & build** review layout stacks **Build summary** under **Preview pictures** (removed `rw-env-review-layout--split`; dropped secondary column max-height). Ran `node tests/room-wizard-results-contract.test.js`, `node tests/room-wizard-environment-copilot.test.js` (Pass); browser layout not re-checked in CI.

Update 2026-04-06: Build summary **runtime review** sits in `rw-runtime-review-row` above `rw-environment-asset-grid`, **click opens game** via delegated handler; removed **Open room in game** button from preview actions. Ran `node tests/room-wizard-results-contract.test.js`, `node tests/room-wizard-environment-copilot.test.js` (Pass); browser not re-checked in CI.

Update 2026-04-06: Runtime review screenshot uses **full panel width** (`width: 100%`, `object-fit: contain`, `max-height: min(72vh, 880px)`). Ran `node tests/room-wizard-results-contract.test.js` (Pass); browser not re-checked in CI.

Update 2026-04-06: Per-slot **Regenerate** / **Iterate** on build summary thumbnails; `POST .../environment/generate-assets` accepts `slot_id` and `iterate_from_current` (Gemini prepends current PNG). Ran `python3 tests/room_environment_system.test.py` (Pass), `node tests/room-wizard-results-contract.test.js`, `node tests/room-wizard-environment-copilot.test.js` (Pass); browser flow not re-checked in CI.

Update 2026-04-06: Bespoke **ceiling_band** / **ceiling_piece** runtime compositing in `index.html` (`addRoomBespokeCeilingDecor` from `buildEnvironmentSetDressing`, alias map in `getRoomEnvironmentBespokeAsset`). Ran `node tests/game-logic.test.js` (Pass); in-browser runtime review and playtest not re-checked in CI.

Update 2026-04-06: Ceiling **Y placement** fix — planner `placement.y` 0 is not world Y; `addRoomBespokeCeilingDecor` now uses `getRoomPolygonBounds` **chamberTop** and centers on chamber width (matches background footprint). Ran `node tests/game-logic.test.js` (Pass); browser not re-checked.

Update 2026-04-06: Runtime review **letterboxing** — headless window used full room size while Phaser internal size matched footprint chamber, so Scale.FIT drew black bars; `_capture_runtime_review_screenshot` now sizes Chrome to **chamber_width × chamber_height** (mins 800×400). `index.html` adds `applyRuntimeReviewCaptureContainerAspect` + camera center clamp; capture-mode ceiling alpha 0.88. Ran `node tests/game-logic.test.js` and `python3 tests/room_environment_system.test.py` `test_runtime_review_capture_uses_browser_by_default_when_available` (Pass); full Python suite not re-run.

Update 2026-04-06: **Ceiling slab read** — runtime shortens background + midground from the top when a loaded bespoke ceiling exists (`getRoomBespokeCeilingCapHeightForRuntime`, `vaultHeight`), skips top feather; Python `_composite_runtime_asset_sprite` no longer fades/offsets `ceiling_band`; `ceiling_piece` prompt discourages collage-like arched window rows. Ran `node tests/game-logic.test.js` and targeted `room_environment_system` composite tests (Pass); full Python suite not re-run.

Update 2026-04-07: **Ceiling prompt contract** — `scripts/room_environment_system.py`: dedicated `ceiling_band` component rules, tighter default ceiling schema (anti-arcade / single-slab), `ceiling_fields` appended to bespoke ceiling prompts, stronger `ceiling_piece` biome role, retry instructions for `ceiling_band` and `ceiling_piece`. Added `test_bespoke_prompt_ceiling_band_rejects_composite_arcade_read`. Ran `python3 tests/room_environment_system.test.py` (122 tests, Pass, sandbox/no network) and `node --test tests/game-logic.test.js` (Pass). Browser acceptance tests not re-run.

Update 2026-04-07: **Room footprint silhouette for Gemini scenic bespoke** — `scripts/room_environment_system.py`: `_write_bespoke_room_silhouette_reference` + prepend to refs for `background_far_plate` / `midground_side_frame` when `room` is passed; `_build_bespoke_prompt(..., room_geometry=...)` adds footprint clause; validation drift checks still use biome template. Decision **#169**. Ran `python3 tests/room_environment_system.test.py` (125 tests, Pass, no network) and `node --test tests/game-logic.test.js` (Pass). Browser acceptance tests not re-run.

Update 2026-04-07: **Unified room shell (opt-in)** — `MV_ROOM_SHELL_FOREGROUND=1`: v2/v3 planners add `room_shell_foreground` (chamber AABB × `foreground_frame` template), skip legacy `ceiling_band` / wall modules / trims / `main_floor_*`; post-Gemini `_apply_walkable_interior_punchout` + `_validate_room_shell_after_punchout`. Runtime: `index.html` places one chamber sprite and skips bespoke ceiling + primary-floor merge + modular wall shell when manifest has the slot. Synthetic punchout QA PNG: `artifacts/qa/room-shell-punchout-synthetic.png` (visually inspected: dark frame band with cyan inner outline, interior cleared to transparent). Decision **#170**. Ran `python3 tests/room_environment_system.test.py` (127 tests, Pass) and `node --test tests/game-logic.test.js` (Pass). Browser playtest not re-run in CI.

Update 2026-04-07 (later): **Room shell thickness + Python runtime review** — Footprint silhouette outline switched to **neutral gray** (reduces model copying accent cyan as a hairline frame); `room_shell_foreground` prompts require heavy masonry mass; optional `MV_ROOM_SHELL_PUNCH_INSET_PX` (documented in `.env.local.example`); `_composite_runtime_review_image` composites `room_shell_foreground` at chamber bounds with `foreground` sort order between midground and floor. Decision **#170b**. Ran `python3 tests/room_environment_system.test.py` (131 tests, Pass; sandbox, network off, Gemini keys unset) and `node --test tests/game-logic.test.js` (Pass). New PNG output from Gemini was not visually re-validated in this run (honesty gate).

Update 2026-04-07 (later): **`shell_rim_mass_low`** — `_validate_room_shell_after_punchout` rejects images with **opaque pixel coverage** below **1.8%** (hairline / outline-only shells after punchout); `_retry_prompt_for_validation_errors` adds a targeted retry for that code. Ran `python3 tests/room_environment_system.test.py` (133 tests, Pass; sandbox, keys unset) and `node --test tests/game-logic.test.js` (Pass). Browser not re-run.

Update 2026-04-07 (later): **Room wizard asset popup** — `room-layout-editor.html`: `openRoomEnvironmentAssetPreviewWindow` opens `room-environment-preview-full.html` in a centered `window.open` popup; preview gallery + build-summary slot thumbnails use `data-rw-asset-src`; runtime review splits **view full size** vs **Open in game**. `room-wizard-workbench-shell.css`: button resets for `.rw-preview-card-open` / `.rw-environment-asset-open`. Ran `node tests/room-wizard-results-contract.test.js` and `node --test tests/game-logic.test.js` (Pass). Manual browser popup not re-checked in CI.

Update 2026-04-07 (later): **Prompts — anti-cyan rim + contrast** — `scripts/room_environment_system.py`: `_build_biome_template_prompt` base rules + `border_piece` / `foreground_frame` template text; bespoke `component_rules` for `background_far_plate`, `midground_side_frame`, `room_shell_foreground`. Decision **#170c**. Ran `python3 tests/room_environment_system.test.py` (sandbox, keys unset). New Gemini output not visually validated in CI.

Update 2026-04-07 (later): **Foreground-frame validation + synthetic fixtures aligned with 3× border geometry** — `scripts/room_environment_system.py` now samples wall collapse luminance between top/bottom frame caps so full-width top caps do not mask missing side walls, and `tests/room_environment_system.test.py` synthetic images for missing-top/lower-edge-break were adjusted to target the validator’s 20% top-band windows under the thicker frame. Ran `python3 tests/room_environment_system.test.py` (133 tests, Pass; sandbox/no network, Gemini calls expected to fail-fast without keys).

Update 2026-04-07 (later): **Border-piece design language + thickness gate** — `scripts/room_environment_system.py` now enforces a heavier `border_piece` read (side thickness floor + top/bottom thickness occupancy checks) and rejects mixed border-band texture families (`border_piece_texture_family_drift`). Border-piece prompts/retries now require one cohesive masonry family across all four bands and explicitly ask for a double-thick enclosing frame read. Added prompt/retry assertions in `tests/room_environment_system.test.py`; ran `python3 tests/room_environment_system.test.py` (133 tests, Pass).

Update 2026-04-07 (later): **Room shell readability pass (thickness + contrast + uniform masonry language)** — `scripts/room_environment_system.py`: default `MV_ROOM_SHELL_PUNCH_INSET_PX` now `20` (heavier surviving rim after punchout), `_validate_room_shell_after_punchout` adds stronger rim-mass floor plus dark-tone and band-drift checks (`shell_tone_too_dark`, `shell_band_tone_drift`), and `room_shell_foreground` prompt/retry text now demands thicker bands, uniform masonry language, and stronger value separation from dark backgrounds. Added `test_validate_room_shell_after_punchout_rejects_near_black_shell_tone`; ran `python3 tests/room_environment_system.test.py` (134 tests, Pass). Regenerated live asset pack and visually inspected `tools/2d-sprite-and-animation/projects-data/test-40b4b333/room_environment_assets/R1/bespoke/R1-room-shell.png` and `.../review/runtime-review.png`.

Update 2026-04-07 (later): **Room shell tone normalization** — after punchout, `scripts/room_environment_system.py` now applies `_normalize_room_shell_tone(...)` to lift very dark shell pixels into a readable mid-dark range before final shell validation. Kept `shell_tone_too_dark` and relaxed `shell_band_tone_drift` tolerance to avoid rejecting otherwise cohesive shells due minor band variance. Ran `python3 tests/room_environment_system.test.py` (134 tests, Pass). Live regen status showed shell slot pass (`room_shell_foreground` generated as AI, validation pass) and saved `R1-room-shell.png` was visually inspected.

Update 2026-04-07 (later): **Composite-look reduction for shell ceiling/corners** — tightened `room_shell_foreground` prompt with a monolithic-ceiling + fused-corner continuity contract, added shell validator signals (`shell_ceiling_composite_read`, `shell_corner_join_seam_read`), and added targeted retries for those error codes in `_retry_prompt_for_validation_errors`. Added test `test_room_shell_retry_prompts_address_ceiling_and_corner_composite_read`; ran `python3 tests/room_environment_system.test.py` (135 tests, Pass). Live regeneration + visual check performed on `.../bespoke/R1-room-shell.png` and `.../review/runtime-review.png`.

Update 2026-04-07 (later): **Corner shadow softening** — added deterministic `_soften_room_shell_corner_shadow(...)` after punchout/normalization so corners do not pool into near-black; prompt + retry now explicitly reject heavy corner vignette (`shell_corner_shadow_pool`). Ran `python3 tests/room_environment_system.test.py` (135 tests, Pass), restarted server, regenerated, and visually inspected saved `.../bespoke/R1-room-shell.png` plus `.../review/runtime-review.png`.
Update 2026-04-07 (later): **Border detail-scale recovery** — tightened `room_shell_foreground` prompt/retry for medium-to-fine masonry (reject oversized block scale), added `shell_detail_scale_too_coarse` validator signal with stricter shell-band contrast thresholds, and applied subtle `_enhance_room_shell_micro_detail(...)` after punchout tone normalization. Ran `python3 tests/room_environment_system.test.py` (135 tests, Pass), regenerated assets, and visually inspected `.../bespoke/R1-room-shell.png` and `.../review/runtime-review.png` for smaller/finer border texture cadence.
Update 2026-04-07 (later): **Two-reference shell conditioning (silhouette + approved preview)** — `room_shell_foreground` generation now uses exactly two references in order: silhouette map first for geometry/shape, approved room preview second for palette/tone/theme only. Prompt explicitly tells Gemini to follow silhouette for shell geometry and avoid copying scene composition from preview. Added `test_room_shell_references_use_silhouette_and_preview`; ran `python3 tests/room_environment_system.test.py` (136 tests, Pass), regenerated assets, and verified attempt metadata lists exactly two shell refs (`.../room_shell_foreground-silhouette.png`, `.../room_environment_previews/...png`).
Update 2026-04-07 (later): **Shell border-gap quality gate (pre + post processing)** — added `_validate_room_shell_before_punchout(...)` for pre-punchout ledge/corner fill checks and extended `_validate_room_shell_after_punchout(...)` with alpha occupancy checks for top-edge continuity and corner mass retention after deterministic processing. Added retry mappings for `shell_top_edge_gap_pre/post` and `shell_corner_gap_pre/post`, plus prompt edge-fill contract language. Added tests: `test_validate_room_shell_before_punchout_rejects_top_edge_gap`, `test_validate_room_shell_after_punchout_rejects_corner_gap`, and retry prompt assertions; ran `python3 tests/room_environment_system.test.py` (138 tests, Pass). Regeneration now blocks malformed shells with explicit gap errors instead of passing visual defects.
Update 2026-04-07 (later): **Test-suite Gemini quota guard** — `tests/room_environment_system.test.py` now stubs `_gemini_generate_content_rest` by default for non-`test_gemini_*` cases, returning deterministic local payloads (including inline PNG data for image requests) so unit tests do not make live Gemini calls or burn tokens. `test_gemini_*` methods still use the real function path for transport/error behavior assertions. Ran `python3 tests/room_environment_system.test.py` (138 tests, Pass).
Update 2026-04-07 (later): **Runtime-review fade removal (no background fainting)** — `index.html` runtime review capture no longer forces lower alpha on background/midground/feather layers or ceiling slab. Regenerated environment assets with Gemini and produced a fresh runtime screenshot at `tools/2d-sprite-and-animation/projects-data/test-40b4b333/room_environment_assets/R1/review/runtime-review.png` (headless browser mode). Full acceptance and browser playthrough tests were not re-run for this visual tweak.
Update 2026-04-07 (later): **Removed cyan from layout conditioning reference** — `scripts/room_environment_system.py` `_render_room_layout_conditioning_image` now uses neutral gray frame/polygon outlines (no cyan UI stroke), reducing cyan leakage into Gemini-generated biome assets. Added regression `test_layout_conditioning_reference_avoids_cyan_outline` and regenerated runtime screenshot: `tools/2d-sprite-and-animation/projects-data/test-40b4b333/room_environment_assets/R1/review/runtime-review.png`. Note: full Python suite still has one unrelated pre-existing failure (`test_validate_room_shell_before_punchout_rejects_top_edge_gap`); targeted new test passes.
Update 2026-04-07 (later): **Cyan rim — source-only fix (no shell post-process)** — removed `_sanitize_room_shell_preview_reference` and `_neutralize_room_shell_edge_cyan`; `room_shell_foreground` again passes the approved preview path unchanged. Fixed remaining **code-generated** cyan in `_render_level1_image` fallback preview (frame outline now neutral gray, not accent cyan). Kept neutral `_render_room_layout_conditioning_image` frames from prior change. Added `test_level1_fallback_preview_has_no_accent_cyan_outline`. Targeted `room_environment_system` tests for layout + level1 + shell refs pass.
Update 2026-04-07: **Runtime review thumbnail border (UI)** — `room-wizard-workbench-shell.css`: `.rw-runtime-review-thumb img` uses neutral white-opacity borders (`rgba(255,255,255,0.08)` / hover `0.16`) instead of `var(--line)` so the workbench does not paint a cyan-tinted rim around the screenshot (reduces perceived “wrong tone” next to the image). Ran `node tests/room-wizard-results-contract.test.js` (Pass). `node --test tests/game-logic.test.js` not re-run for this CSS-only change; current `index.html` fails `testIndexPreviewStartFromHashSmoke` assertion on backdrop-weight string (drift vs test expectation).
Update 2026-04-07: **Room-shell prompt contract reset to technical-only scaffolding** — `scripts/room_environment_system.py` now keeps `room_shell_foreground` guidance focused on geometry/occupancy/thickness/masonry continuity and avoids template-level composition steering for that slot. The room-shell composition line in `_build_bespoke_prompt` is now component-scoped and does not inject focal-scene style direction; silhouette+approved preview contract remains (silhouette for shape, approved preview for palette/theme context). Ran `python3 tests/room_environment_system.test.py` (140 tests total, 1 known existing failure: `test_validate_room_shell_before_punchout_rejects_top_edge_gap`).
Update 2026-04-07: **Major regression pass — shell iteration refs + runtime capture tone alignment** — `scripts/room_environment_system.py` now keeps `room_shell_foreground` reference order stable during iterate (`silhouette`, `approved preview`, then current asset) instead of prepending current asset first; shell prompt opening now explicitly references silhouette+approved preview attachments for this slot. `index.html` runtime-review capture mode now uses a neutral camera clear, disables starfield, raises bespoke background/midground alpha, and lowers feather overlays so background tone is readable and closer to shell masonry family in review screenshots. Updated `tests/game-logic.test.js` smoke assertion for the new capture backdrop expression. Ran `python3 tests/room_environment_system.test.py` (140 tests, 1 known existing failure: `test_validate_room_shell_before_punchout_rejects_top_edge_gap`) and `node --test tests/game-logic.test.js` (Pass).
Update 2026-04-07: **QA review + targeted shell re-gen (R1)** — ran `generate_project_room_environment_assets(..., {'slot_id':'R1-room-shell','iterate_from_current': true})` after prompt/ref-order changes; `room_layout.json` attempt log confirms shell refs in order `silhouette -> approved preview -> current asset` and shell-specific prompt opening. QA follow-up on saved `runtime-review.png` reports room-shell ref/prompt checks **Pass**, with residual warning `floor_background_separation_low` (background still reads soft/faded relative to foreground shell). No acceptance checklist changes proposed.
Update 2026-04-07 (later): **Room-shell edge-fill + JSON spatial contract** — `scripts/room_environment_system.py` adds `_room_shell_spatial_contract_json` (bbox of shell band, mask edge-touch flags, `edge_flush_rule`) appended to `room_shell_foreground` prompts; strengthens `room_shell_foreground` text with EDGE-FILL CONTRACT and anti–picture-in-picture rules. Tests: `test_room_shell_spatial_contract_json_matches_mask_geometry`, `test_build_bespoke_prompt_room_shell_includes_masonry_thickness_rules` (importlib runner, Pass). Full `python3 tests/room_environment_system.test.py` not re-run in this session (suite is long); browser/Gemini regeneration not re-run.
Update 2026-04-07 (later): **Room-shell bespoke prompt split from template-adaptation** — `room_shell_foreground` now uses `_build_bespoke_prompt_room_shell_foreground` so the prompt no longer injects “adapt the template,” full walls `design_intent` (hall/arches), or “compose fog, depth, and architecture” from the shared footprint block. Mask-first CRITICAL block + minimal material schema. Tests: `test_build_bespoke_prompt_room_shell_includes_masonry_thickness_rules` (mask paint / negated strings), `test_build_bespoke_prompt_includes_footprint_clause_when_geometry_has_polygon` (assertion aligned to “room footprint conditioning”); importlib runner, Pass. Full suite not re-run.
Update 2026-04-07 (later): **`shell_ceiling_composite_read` threshold** — `scripts/room_environment_system.py`: `ROOM_SHELL_CEILING_BAND_MEAN_CONTRAST_MAX` (0.035) replaces the hardcoded 0.024 ceiling-band contrast cap so mask-driven masonry texture is less likely to false-fail after `template_family_drift` was removed for `room_shell_foreground`. Ran `python3 tests/room_environment_system.test.py RoomEnvironmentSystemTests.test_room_shell_retry_prompts_address_ceiling_and_corner_composite_read RoomEnvironmentSystemTests.test_validate_room_shell_after_punchout_rejects_hairline_mass` (Pass). Full `room_environment_system` suite not re-run in this session (long-running / environment-dependent).
Update 2026-04-07 (later): **Bespoke build wait bar — Gemini duration model** — `room-layout-editor.html`: added `roomWizardBespokeComponentUsesGemini` / `roomWizardEstimateBespokeGeminiSlotCount` / `roomWizardEstimateBespokeAssetWaitMs` (formula: slots × 8s × 12 multiref factor × 1.45 expected attempts + 40s tail; 8s from published ~3–8s Flash Image light-request band). Full build and per-slot regen/iterate pass `estimatedDurationMs` into `startRoomWizardWaitbar` so progress reaches ~92% over the estimate (linear). Status copy includes rounded minutes + slot count. Ran `node tests/room-wizard-results-contract.test.js` (Pass). Browser not exercised in CI.
Update 2026-04-07 (later): **Unified bespoke shell — interior wall colliders** — `index.html`: `addRoomBespokeShellInteriorSideColliders` adds 1–2 columns of `createRoomSurfaceTile` wall physics just inside `chamberBounds` left/right after unified shell decor, so polygon-outside wall tiles + shell art bleed no longer leave a walkable gap inside the visible masonry (playtest R1 knight clipping into left wall). Added `testIndexUnifiedShellInteriorSideColliders` in `tests/game-logic.test.js`. Ran `node --test tests/game-logic.test.js` (Pass). User-provided playtest screenshot inspected (knight overlapping left stone shell).
Update 2026-04-07 (later): **Unified shell interior collider width (scaled lip)** — `addRoomBespokeShellInteriorSideColliders` now sets lip depth from `room_shell_foreground` display width × `FOREGROUND_FRAME_BORDER_SIDE_PX`/`ATLAS_WIDTH` (mirrored as `SHELL_UNIFIED_SIDE_COLLIDER_*` in `index.html`) × `SHELL_UNIFIED_SIDE_COLLIDER_BAND_USE`, clamped 20–56px per side and capped so left/right strips do not overlap; invisible wall-texture sprites use `setDisplaySize(lipPx, bodyH)` instead of fixed 32/64px tile stacks. Updated `testIndexUnifiedShellInteriorSideColliders`. Ran `node --test tests/game-logic.test.js` (Pass). Browser playtest not re-run in CI.
Update 2026-04-07 (later): **Room env refs — remove white halo sources** — `scripts/room_environment_system.py`: bespoke `room_shell` silhouette PNGs use `ROOM_SHELL_SILHOUETTE_CLEAR_RGB` (8,12,16) instead of white margins/interior so Gemini is not taught paper-white rims; spatial-contract JSON + shell prompts updated (neutral-dark keep-clear, forbid #FFFFFF halos). Layout conditioning door outline and level-3 preview door stroke no longer use near-white. Ephemeral `foreground_frame` / `border_piece` generation guides use darker frame fills and no thick near-white seam line. Tests: `test_bespoke_room_silhouette_reference_uses_dark_clear_not_white`, spatial contract key assertion, relaxed `test_foreground_frame_generation_guide_emphasizes_cap_and_floor_bands` (cap/floor vs wall contrast without paper-white). Targeted unittest runs Pass; full `python3 tests/room_environment_system.test.py` not completed in agent timebox (long suite).
Update 2026-04-08: **Room environment test runtime reduction (Gemini + render hot paths)** — `scripts/room_environment_system.py` now routes `_call_gemini_image` and `_gemini_json` through `_gemini_generate_content_rest` (with optional `system_instruction` and `generation_config_merge`) so one test stub intercepts all Gemini traffic and avoids slow direct `urlopen` paths; `tests/room_environment_system.test.py` default Gemini stub now accepts extra kwargs, and most tests stub heavyweight renderers (`_render_art_direction_fallback`, `_render_level1_image`, `_render_level2_image`, `_render_level3_image`, `_render_room_layout_conditioning_image`) to tiny deterministic images while preserving two pixel-contract tests as exemptions. Full suite timing re-benchmark is pending due long-running/aborted local runner sessions in this environment.
Update 2026-04-08 (later): **Room env suite speedup via reference-guide fast path in tests** — `tests/room_environment_system.test.py` now stubs `_bespoke_reference_images_for_component` for non-reference-sensitive tests (method names not containing `reference`/`guide`/`silhouette`/`layout_conditioning`/`level1_fallback_preview`) to skip expensive guide-image synthesis while preserving coverage for reference-contract tests. Re-benchmark: `test_generate_and_approve_previews` improved from **230.62s** to **2.25s**; prior 3-test subset (`generate_and_approve_previews` + two Gemini transport tests) improved from **216.13s** to **2.17s**. Full `pytest tests/room_environment_system.test.py -q` now completes in **155.09s** with **145 passed / 2 failed**. The two failures are room-shell validation expectation drift (`test_room_shell_silhouette_band_mask_creates_border_zone`, `test_validate_room_shell_after_punchout_rejects_near_black_shell_tone`) and are not in the optimized fast-path area.
Update 2026-04-08 (later): **Expectation-only test alignment (no production logic changes)** — updated two room-shell tests to match current validator contract: `test_room_shell_silhouette_band_mask_creates_border_zone` now asserts non-empty border occupancy instead of a hardcoded corner pixel, and `test_validate_room_shell_after_punchout_rejects_near_black_shell_tone` now applies punchout before post-punchout validation and asserts rejection without requiring deprecated `shell_tone_too_dark`. Re-ran `pytest tests/room_environment_system.test.py -q` → **147 passed in 156.91s**.

Update 2026-04-08 (later): **Structural bespoke footprint silhouette + prompts** — `scripts/room_environment_system.py` prepends `_write_bespoke_room_silhouette_reference` for all structural bespoke slots when `room` has a valid polygon (same `[silhouette, template, guide]` order as scenic slots); `_build_bespoke_prompt` applies the “Room footprint conditioning” clause to wall/floor/ceiling/pit structural component types when `room_geometry` has a polygon. Documented in `decisions/2026-03-31-room-environment-quality-pass.md` as decision **#18**. Re-ran `pytest tests/room_environment_system.test.py -q` → **147 passed in ~163s**. Live Gemini regeneration not re-run; output faithfulness not visually validated in this session.

Update 2026-04-08 (later): **Playtest primary floor follows polygon bottom profile** — `index.html`: added `getRoomPolygonPrimaryFloorSpans` (per-column max interior Y → snapped tile centers, merged runs) so primary-floor collision, face/band decor, and `main_floor_top` cap draw as one or more horizontal segments at the correct Y instead of a single full-bbox strip; `shellFloorTileCenterY` uses the deepest span for shell side colliders. Updated `tests/game-logic.test.js` string assertions. Ran `node --test tests/game-logic.test.js` (Pass). Manual playtest not re-run in CI.

Update 2026-04-08 (later): **Revert unified-shell floor depth override** — Reverted commit `6518605c` (“draw primary floor cap above unified room shell”): removed optional `depthOverride` parameters on primary floor cap/band/face helpers and restored fixed depths (`main_floor_top` cap **0.08**, etc.). Founder reported an unwanted cyan-looking border after that change; the reverted commit only adjusted z-order, not stroke color. Ran `node --test tests/game-logic.test.js` (Pass). Browser/playtest not re-run in CI.

Update 2026-04-08 (later): **Room editor: browser save survives refresh when server layout exists** — `room-layout-editor.html`: when `/api/layout` (or project layout API) succeeds, the loader previously always replaced state with the server JSON, so localStorage was ignored on refresh. Added `getLayoutPreferBrowserKey()`; **Save**, dirty navigate persist, and `updateJsonText()` (runs after redraw) set the flag and keep localStorage current; load prefers that copy when the flag is set until **Sync canonical**, **Reload from disk**, or **Clear Scratch Save**. Ran `node tests/room-wizard-results-contract.test.js` and `node --test tests/game-logic.test.js` (Pass). Browser manual refresh not re-run in CI.

Update 2026-04-08 (later): **Build summary: tip when runtime review fails on browser capture** — `room-layout-editor.html`: when `runtime_review.fail_reasons` includes `browser_capture_required`, **Build details** copy now appends a short tip (`ROOM_ENVIRONMENT_REVIEW_BROWSER`, `ROOM_ENVIRONMENT_REVIEW_BASE_URL`, composite fallback). Ran `node tests/room-wizard-results-contract.test.js` (Pass).

Update 2026-04-08 (later): **Unified shell footprint mask (notched/stepped polygons)** — `index.html`: added `applyUnifiedShellFootprintMask` so `room_shell_foreground` is clipped with a geometry mask from a thick polygon stroke in world space, hiding AABB bleed into void/notches. Documented as decision **#171** in `decisions/2026-03-31-room-environment-quality-pass.md`. Ran `node --test tests/game-logic.test.js` (Pass). In-browser playtest not re-run in CI.

Update 2026-04-09: **Handover doc only** — Added `docs/handover-room-shell-fidelity-fix.md` (Level Design + QA review: unified shell pipeline findings, plan alignment, AC, approvals). No product code changed; automated tests not re-run for this documentation-only change.

Update 2026-04-09 (later): **Art bible v0.3** — Added `artifacts/ashen-hollow-art-bible-v0.3.md` per `docs/handover-art-bible-v0.3-spec.md`; regenerated palette swatches (`scripts/generate_art_bible_swatches.py`, 15 tokens); updated `docs/pixel-art-quality-standards.md`, `AGENTS.md`, `creative-status.json`, `scripts/build_os_document_library.py` catalog entries, `artifacts/ashen-hollow-art-bible-v0.1.md` stub. Ran `python3 tests/art_bible_swatches.test.py` (Pass), `python3 scripts/validate_status_files.py creative-status.json` (Pass), `python3 tests/os_document_library.test.py` (Pass), `node --test tests/game-logic.test.js` (Pass). Acceptance tests (AT-01+) not re-run; change is documentation and asset metadata.

Update 2026-04-09 (later): **Art bible PDF — STYLE_GUIDE dark export** — `scripts/send_weekly_digest.py` adds `md_to_html(..., theme=\"styleguide\")` and `wrap_html(..., theme=\"styleguide\")` using `STYLE_GUIDE.md` tokens; `scripts/send_markdown_pdf_email.py` uses that theme for PDF + notification HTML (`print-color-adjust`, dark body, cyan accent strip, pixelated images). Synced `agent-os-standalone/scripts/send_weekly_digest.py`. `design-status.json` priority #5. Ran `python3 tests/send_weekly_digest_email_theme_test.py` (Pass), `python3 scripts/validate_status_files.py design-status.json` (Pass). Resend of art bible v0.3 PDF to founder via Resend (manual). AT-01+ not re-run.

Update 2026-04-09 (later): **Design memo — art bible PDF / email** — Added `docs/reports/design-review-art-bible-pdf-export-2026-04-09.md` (Outlook/large-attachment guidance, §5 delivery options). `send_markdown_pdf_email.py`: `--pdf-only` (no Resend), stderr **WARNING** when PDF ≥10 MB. Updated `design-status.json` founder decision + design-review action; `build_os_document_library.py` formal title; regenerated `docs/os-document-library.html`. Ran `python3 tests/os_document_library.test.py` (Pass), `validate_status_files.py design-status.json` (Pass). AT-01+ not re-run.

Update 2026-04-10: **Art bible v0.3.1 — per-biome wall/platform textures** — `artifacts/ashen-hollow-art-bible-v0.3.md`: §4.3 global material vocabulary only; five `art-bible/concepts/biome-0N-walls-platforms.svg` plates + biome copy for wall / platform / border; legacy material board superseded. `creative-status.json` note; `artifacts/ashen-hollow-art-bible-v0.1.md` + `artifacts/concepts/README.md` pointers. Ran `python3 scripts/validate_status_files.py creative-status.json` (Pass). AT-01+ not re-run.

Update 2026-04-09 (later): **Map MVP plan — expert subagents + research evidence** — `prompts/project_plan.md`: added “Execution standard — expert subagents, training, research, and evidence” (Development/QA ownership, subagent selection table, pre-flight research, evidence pack). No product code changed. Ran `node --test tests/game-logic.test.js` (Pass). AT-01+ not re-run.

Update 2026-04-09 (later): **Handover doc** — `docs/handover-map-mvp-subagent-research-evidence.md` (same execution standard + git ref `a65710bb`); `prompts/project_plan.md` links to handover. No product code changed. Ran `node --test tests/game-logic.test.js` (Pass). AT-01+ not re-run.

Update 2026-04-10: **Intense dev orchestration** — Added `docs/skills/intense-dev-orchestration.md` (canonical multi-agent workflow for Cursor / Claude / Codex entry skills). Documentation only; automated test suites and AT-01+ not re-run.

Update 2026-04-11: **Room environment batch (shell punchout, background QA, prompt parity)** — `scripts/room_environment_system.py`: shell alpha clamp to band mask; background vertical-bar validation + retry; `_restore_background_shell_definition` no-op; `background_far_plate` footprint prompt clause when polygon refs exist (decision §196); midground review isolation flag and related paths. `tests/room_environment_system.test.py` + decision log updated. Ran `python3 -m pytest -q tests/room_environment_system.test.py` (174 passed, ~62s). AT-01+ and manual R1 regeneration not re-run in CI.

Update 2026-04-11 (later): **Runtime review — unified shell draw order in capture mode** — `index.html`: `RUNTIME_REVIEW_CAPTURE_MODE` unified shell depth **0.1 → 0.16** so platform ledges (0.11) no longer paint over the frame; decision §197. `tests/game-logic.test.js`: unified-shell assertion aligned with `mask.invertAlpha = invertAlpha`. Ran `node --test tests/game-logic.test.js` (Pass). Full R1 asset regen after the change hit `top_occlusion_slab_present` once (likely scenic/metric variance); AT-01+ not re-run.

Update 2026-04-11 (later): **Runtime review — skip unified-shell footprint mask in capture** — `index.html`: `applyUnifiedShellFootprintMask` returns early when `RUNTIME_REVIEW_CAPTURE_MODE` so QA PNG matches saved shell art (decision §198). `tests/game-logic.test.js` updated. Ran `node --test tests/game-logic.test.js` (Pass); `_run_runtime_review` smoke on R1 (**pass**, headless_browser). AT-01+ not re-run.

Update 2026-04-11 (later): **Playtest parity — remove unified-shell footprint mask entirely** — `index.html`: deleted `applyUnifiedShellFootprintMask`; unified shell depth **0.16** for both play and capture so workbench playtest matches Build Summary / `runtime-review.png` (decision §199, §198 superseded). `tests/game-logic.test.js`: `testIndexUnifiedShellSkipsPolygonMaskForPlaytestParity`. Ran `node --test tests/game-logic.test.js` (Pass). AT-01+ not re-run.

Update 2026-04-11 (later): **Unified shell placement tracks polygon AABB** — `index.html`: `computeUnifiedShellWorldPlacement(asset, support)` sets display size to current `getRoomPolygonBounds` span and position from origins (`localX = curLeft + ox * dw`, `localY = curTop + oy * dh`); ignores stale manifest `placement.x/y` and display sizes (decision §200). `tests/game-logic.test.js` extended. Ran `node tests/game-logic.test.js` (Pass). Playtest shell vs footprint not visually re-validated in this run.

Update 2026-04-11 (later): **Unified shell opening centroid nudge** — `index.html`: `getUnifiedShellOpeningCentroidTexture` + shift after AABB placement so alpha0 opening centroid aligns to chamber center (decision §201; fixes asymmetric L-shell mass vs image center). `computeUnifiedShellWorldPlacement(scene, asset, support)`. Ran `node tests/game-logic.test.js` (Pass). Playtest not visually re-checked.

Update 2026-04-12: **Unified shell: inverted-alpha polygon mask replaces centroid nudge (§202)** — Root cause analysis: raster scan proved the shell PNG opening geometry is 400–800px wrong vs the polygon contract (AI generation artifact). No placement shift can compensate. Fix: `index.html` removes `getUnifiedShellOpeningCentroidTexture`, reverts `computeUnifiedShellWorldPlacement` to `(asset, support)` (no scene, no nudge, `const localX`), and applies `ensureRoomFootprintGeometryMask(this, roomId, roomBounds, true)` (inverted-alpha) to the shell sprite in both play and capture for pixel-accurate alignment + mode parity. `tests/game-logic.test.js`: renamed test to `testIndexUnifiedShellUsesInvertedPolygonMaskForParity`. Ran `node tests/game-logic.test.js` (Pass). **Founder confirmed same visual in playtest** — GeometryMask invertAlpha did not clip in WebGL embed. Superseded by §202b.

Update 2026-04-12 (later): **Unified shell: canvas-premasked texture via destination-out (§202b)** — §202's `GeometryMask.invertAlpha` worked in headless capture but not in the live playtest WebGL path. Fix: `getOrCreatePremaskedShellTexture(scene, slotId, roomId)` draws the shell onto a temp HTML Canvas, uses `globalCompositeOperation = 'destination-out'` to erase the polygon interior (mapped from room-local coords to texture space), and registers the result as a new Phaser texture. The shell sprite uses this pre-masked texture directly — no Phaser mask object. Removes the `ensureRoomFootprintGeometryMask` call on the shell. `tests/game-logic.test.js`: `testIndexUnifiedShellUsesPremaskedTexture`. Ran `node tests/game-logic.test.js` (Pass). Visual validation pending founder playtest.

Update 2026-04-12: **Room editor layout vs Environment overlay alignment (decision §203)** — `room-layout-editor.html`: `roomScale()` uses uniform `Math.min(scaleX, scaleY)` (letterboxed square pixels); `roomWizardOverlayBounds` prefers assembly `size`, `room_semantics.room_size`, wizard room `size`, then polygon span; overlay copy clarifies cyan footprint vs gold structural slots; thinner SVG strokes for polygon/shell lines. `tests/room-wizard-overlay-alignment.test.js`; CI runs `room-wizard-results-contract.test.js` + overlay test. Ran `node tests/room-wizard-overlay-alignment.test.js`, `node tests/room-wizard-results-contract.test.js`, `node tests/game-logic.test.js` (Pass). RW-1 manual / Layout overlay not re-run in browser in CI.

Update 2026-04-12 (later): **Minimal bespoke compositor opt-in (decision §204)** — `index.html`: `MINIMAL_BESPOKE_COMPOSITOR` via `?minimalBespokeCompositor=1` or `window.__MV_MINIMAL_BESPOKE_COMPOSITOR`; for bespoke+unified-shell rooms skips background feather sprites; unified shell uses raw texture (no Canvas premask). Plan: `docs/plans/room-bespoke-compositor-minimal.md`. `tests/game-logic.test.js` extended. Ran `node tests/game-logic.test.js` (Pass). Visual gate (minimal vs default) not run in CI; founder must compare playtest frames.

Update 2026-04-12 (later): **Shell premask cache + anti-collage prompt (decision §207)** — `index.html`: `hashRoomFootprintForShellPremask(roomId)`; premask Phaser texture key `env-bespoke-{slotId}-masked-{hash}` so polygon edits do not reuse a stale Canvas punch. `scripts/room_environment_system.py`: `room_shell_foreground` bespoke prompt adds anti-collage / anti–void-gutter language (no flat black stripes through the shell band). `tests/game-logic.test.js`, `tests/room_environment_system.test.py` assertions extended. Ran `node --test tests/game-logic.test.js` and `python3 tests/room_environment_system.test.py` (Pass). Browser playtest and new Gemini regen not exercised in CI; black bands baked into existing PNGs require regen to change.

Update 2026-04-12 (later): **Background plate vs unified shell + runtime height parity (decision §208)** — `scripts/room_environment_system.py`: `background_far_plate` prompt forbids a second perimeter “shell” in the plate (depth-only in void); silhouette clause + retry for `background_shell_definition_low` aligned. `index.html`: `getRoomBespokeCeilingCapHeightForRuntime` returns 0 when unified shell so background/midground `vaultHeight` matches full chamber (same scale as premasked shell). `tests/game-logic.test.js`, `tests/room_environment_system.test.py` updated. Ran `node --test tests/game-logic.test.js` and `python3 tests/room_environment_system.test.py` (Pass). Regenerate `background_far_plate` in workbench for new pixels; browser not re-run in CI.

Update 2026-04-12 (later): **Full pipeline audit follow-up — background contract + runtime placement (decision §209 pending)** — `scripts/room_environment_system.py`: background schema defaults no longer describe an enclosing shell; biome `background_plate` template prompt now forbids a perimeter shell; planner/plan entries use `border_treatment: interior_depth`; `_structural_slot_reference_guide(..., "background_far_plate")` no longer draws a shell-like side-frame/portal diagram. `index.html`: bespoke background uses manifest placement/display size rather than chamber-fit stretch, and footprint masks for background/midground use `ROOM_IDS[roomId]` world bounds instead of the layout room object. `tests/game-logic.test.js`, `tests/room_environment_system.test.py` updated. Ran `node --test tests/game-logic.test.js` and `python3 tests/room_environment_system.test.py` (Pass). Existing bad PNGs still require regeneration to reflect the new generation contract.

Update 2026-04-12 (later): **Missed dependency fixed — v3 planner + editor schema background contract (decision §210)** — `scripts/room_environment_v3.py`: `background_far_plate` now emits `border_treatment: interior_depth` instead of `full_frame`; `room-wizard-environment.js`: background defaults no longer describe an enclosing hall shell and now explicitly reject duplicate perimeter shells. Re-ran `node --test tests/game-logic.test.js` and `python3 tests/room_environment_system.test.py` (Pass). This fix requires a new regeneration to replace manifests/PNGs created under the old `full_frame` contract.
Update 2026-04-12 (later): **Background prompt leak fix — stale saved schema + biome template reference (decision §211)** — `scripts/room_environment_system.py`: background schema normalization now rewrites legacy shell-language fields (`far-depth architectural stone shell`, `enclosing hall shell`, `must read as enclosing shell`) back to the current interior-depth contract; `background_far_plate` prompt opening/composition contract now treats references as material/depth guidance rather than a framed template to preserve; `background_far_plate` bespoke refs now use silhouette + generated guide only (drop live biome `background_plate.png` from the prompt path); validation reference selection follows the generated refs for that slot. `tests/room_environment_system.test.py`: added regressions for stale background schema cleanup, prompt wording, and background reference inputs; updated older tests to match the new two-reference/guide-only contract. Ran targeted `python3 -m pytest -q tests/room_environment_system.test.py -k "background and (normalize or bespoke or prompt or validation or references)"` (10 passed) and full `python3 tests/room_environment_system.test.py` (176 tests, Pass). Existing `R1-background.png` still requires regeneration to reflect the fixed prompt/reference path.

Update 2026-04-12 (later): **Unified shell premask default off (decision §212)** — `index.html`: `UNIFIED_SHELL_FOOTPRINT_PREMASK` / `?shellFootprintPremask=1` enables §202b Canvas punch; default uses raw `env-bespoke-{slotId}` so L-shaped footprints do not lose masonry to `destination-out` mis-registration. `tests/game-logic.test.js` assertions updated. Ran `node --test tests/game-logic.test.js` (Pass). Browser/runtime-review screenshot not re-captured in CI; founder should reload playtest and compare to isolated `R1-room-shell.png`.

Update 2026-04-12 (later): **Unified shell placement locked to polygon AABB top-left (decision §213)** — `index.html`: `computeUnifiedShellWorldPlacement(roomId, asset, support)` uses `getRoomPolygonBounds(roomId)` for `dw`/`dh` and returns `localX`/`localY` as rounded chamber top-left with `setOrigin(0,0)`; manifest `placement.x`/`y` not used. `addRoomBespokeUnifiedShellForegroundDecor` passes `roomId`; unified shell sprite uses `setRoundPixels(true)` when available. `tests/game-logic.test.js` (`testIndexUnifiedShellUsesPremaskedTexture`) updated. Ran `node --test tests/game-logic.test.js` (Pass). In-browser alignment of shell inner edge vs layout footprint grid not re-validated in this run (honesty gate).

Update 2026-04-12 (later): **Unified shell raw PNG: hole bbox drives scale/offset (decision §214)** — `index.html`: `measureShellInteriorHoleBBoxFromTexture` + placement so transparent interior maps to chamber AABB (fixes `R1-room-shell.png` where alpha hole is 966px wide on a 1080px-wide file). Premask path unchanged (1:1 chamber mapping for polygon punch). `tests/game-logic.test.js` updated. Ran `node --test tests/game-logic.test.js` (Pass). Safari playtest not re-run in CI; hole span verified on disk via raster sample (966×960 transparent AABB on 1080×960 PNG).

Update 2026-04-12 (later): **Unified shell hole = largest connected low-alpha component (decision §215)** — `index.html`: `measureShellInteriorHoleBBoxFromTexture` flood-fills so disconnected top transparent sliver + opaque y=106..114 band do not poison minY; R1 main void bbox ~851×731 at ~115,115. Placement still `dw/dh/localX/localY` from hole vs chamber AABB. `tests/game-logic.test.js` asserts `bestArea`. Ran `node --test tests/game-logic.test.js` (Pass). In-browser visual not re-checked in CI.

Update 2026-04-12 (later): **R1 unified shell baseline (decision §216)** — `tests/fixtures/unified_shell_r1_placement_baseline.json`, `tests/unified_shell_hole_baseline.test.py` (synthetic + optional R1 PNG assert), `docs/baselines/unified-shell-placement-r1.md`. CI: `.github/workflows/test.yml` runs Python baseline step. Ran `python3 tests/unified_shell_hole_baseline.test.py` (Pass locally with QA PNG present); CI exercises synthetic path when `projects-data` absent.

Update 2026-04-12 (later): **Runtime review occlusion + background 2D perspective (decision §217)** — `scripts/room_environment_system.py`: `_composite_runtime_review_image` skips left/right/bottom occlusion when `room_shell_foreground` is present (`has_unified_shell`); `background_far_plate` prompt/guide already tightened for orthographic side-scroller read and no duplicate shell in plate. `tests/room_environment_system.test.py`: prompt assertions match `flat depth/layout guide` + orthographic wording. Ran `python3 tests/room_environment_system.test.py` (176 tests, Pass; one suite logs Gemini 400 when API key unset — expected in sandbox). AT-01–AT-09 not run in browser; `runtime-review.png` not visually re-validated in this run (honesty gate).

Update 2026-04-13: **Room editor — refresh / workbench project context** — `room-layout-editor.html`: sessionStorage restores `project_id` in the URL when it was dropped (empty query, no `local_slot`); `loadData` no longer paints embedded seed before the layout API runs; API failure falls back to last browser save; `pagehide` persists dirty layout + prefer-browser flag; successful **Sync canonical** mirrors JSON into the same localStorage key as Save Local. Sidebar copy reminds users to keep `?project_id=…`. Escape hatch: `?no_session_project=1`. Ran `node tests/room-wizard-results-contract.test.js`, `node tests/room-wizard-overlay-alignment.test.js`, `node --test tests/game-logic.test.js` (Pass). AT-01–AT-09 not run; manual refresh with/without `project_id` not exercised in CI.

Update 2026-04-13 (later): **Room preview + art-direction concepts — 2D orthographic (decision §218)** — `scripts/room_environment_system.py`: `_build_level3_variant_prompt` and `_build_art_direction_concept_prompts` now require side-scroller orthographic depth (near-parallel verticals, layered planes not one-point tunnel); extra output negatives for fisheye / corridor matting; variant 1 note aligned. Ran `python3 tests/room_environment_system.test.py` (176 tests, Pass). New preview PNGs not visually validated in CI (honesty gate).

Update 2026-04-13 (later): **Level-3 preview line artifacts + concept influence (decision §219)** — `scripts/room_environment_system.py`: softer `_render_room_layout_conditioning_image` (no labels, muted geometry); level-3 prompt bans schematic/editor overlay lines and instructs frozen concepts as mood/palette only (no camera copy). Tests for `_build_level3_variant_prompt`. Ran `python3 tests/room_environment_system.test.py` (178 tests, Pass). Regenerated preview pixels not visually inspected in CI.

Update 2026-04-13 (later): **Level-3 footprint fidelity + orthographic copy (decision §220)** — conditioning image uses chamber-consistent mapping for polygon/platforms/doors; numeric footprint contract in Gemini prompt; no variant glow. Ran `python3 tests/room_environment_system.test.py` (180 tests, Pass). New previews not visually validated in CI.

Update 2026-04-13 (later): **Level-3 mask + crop (decision §221)** — `_render_room_layout_conditioning_image` is void/fill silhouette mask; `_crop_level3_gemini_output_to_footprint` crops saved preview to footprint AABB (scaled if model output resolution differs). Ran `python3 tests/room_environment_system.test.py` (181 tests, Pass). AT-01–AT-09 not run; regenerated preview PNGs not visually inspected in CI (honesty gate).

Update 2026-04-13 (later): **Level-3 concave alpha punch (decision §222)** — after crop, multiply alpha by walkable polygon in image space so L-shaped rooms (e.g. R1 notch) are transparent outside the true footprint; `MV_LEVEL3_PREVIEW_POLYGON_MASK=0` disables. Ran `python3 tests/room_environment_system.test.py` (182 tests, Pass). R1 preview PNG not visually re-checked in CI (honesty gate).

Update 2026-04-13 (later): **Anti duplicate-shell rim on preview + background plate (decision §223)** — stronger level-3 / `background_far_plate` copy against mask-tracing masonry; default `MV_LEVEL3_PREVIEW_MASK_ERODE_PX=2` erodes footprint mask before alpha multiply (reverts if mask would wipe). Ran `python3 tests/room_environment_system.test.py` (183 tests, Pass). Workbench screenshot case not visually re-run in CI (honesty gate).

Update 2026-04-13 (later): **Runtime review headless capture + composite policy (decision §224)** — `capture_runtime_review.js` WebSocket fallback; dynamic CDP port; Python logging and try/except fix; usable composite no longer forces `browser_capture_required` fail (warning `browser_capture_degraded` instead). Ran `python3 tests/room_environment_system.test.py` (184 tests, Pass). Full headless path not exercised over real Chrome in CI.

Update 2026-04-13 (later): **Runtime review CDP captures top document (§224 follow-up)** — `_write_runtime_review_capture_page` returns repo-root `index.html#…` for headless (wrapper `runtime-capture.html` still written for manual use); `capture_runtime_review.js` targets `index.html` and reads Phaser boot/canvas from the top document when `#preview` iframe is absent. Ran `python3 tests/room_environment_system.test.py` (184 tests, Pass). Real Chrome + Safari workbench screenshot not re-run in CI; AT-01–AT-09 not run.

Update 2026-04-13 (later): **Revert Cursor Cloud Agents room-env PNG integration** — git revert of three commits (Cloud Agents client + UI toggle + waitbar tweak); room previews/bespoke images Gemini-only again; decision §225. Ran `python3 tests/room_environment_system.test.py` (184 tests, Pass), `node tests/room-wizard-results-contract.test.js`, `node --test tests/room-wizard-environment-copilot.test.js` (Pass).

Update 2026-04-13 (later): **Background plate anti–double-shell prompts (decision §226)** — stronger `background_far_plate` copy vs portal/proscenium/near-frame; default component prompt + `enclosure_architecture`; scrub toxic `design_intent`; `room-wizard-environment.js` parity. Ran `python3 tests/room_environment_system.test.py` (185 tests, Pass), `node tests/room-wizard-environment.test.js` (Pass). Regenerated R1 background PNG not visually inspected in CI.

Update 2026-04-13 (later): **`background_far_plate` reference stack guide-only (decision §227)** — `_bespoke_reference_images_for_component` no longer prepends footprint silhouette for far plate (avoids model painting a second rim on top of `room_shell_foreground`); footprint stays in text + depth-tone guide. Tests updated for single-ref background + prompt clauses. Ran `python3 tests/room_environment_system.test.py` (185 tests, Pass; sandbox logs Gemini 400 when key unset). Regenerated workbench PNG not visually inspected in CI (honesty gate). AT-01–AT-09 not run.

Update 2026-04-14: **Founder baseline R1 scenic + unified shell (decision §228)** — `docs/baselines/room-environment-r1-scenic-shell-2026-04-14.md`, `tests/fixtures/room_environment_r1_scenic_baseline.json`, `tests/room_environment_r1_scenic_baseline.test.py`; CI workflow runs the fixture test. Ran `python3 tests/room_environment_r1_scenic_baseline.test.py` (Pass). AT-01–AT-09 not run; optional git tag `baseline/room-environment-r1-scenic-2026-04-14` documented in baseline doc.

Update 2026-04-13: **Room editor modularization (partial vs fizzy-beaming-cray v1.3)** — Inline CSS → `css/`; wizard scripts → `js/wizard/`; inline editor IIFE → `js/editor` chunk loader + `RoomEditor.State` / `RoomEditor.Ui.refs` transforms; `scripts/generate-room-editor-chunks.py` regenerates chunks. Ran `for t in tests/*.test.js; do node "$t"; done` (Pass). §10.2 browser smoke and AT-01–AT-09 not run; HTML line count still dominated by embedded seed JSON (shell-only ≤250 not met).

Update 2026-04-13 (later): **Describe-step Copilot preview mirror** — `renderRoomWizardCopilotPreview` in `js/editor/chunk-2.js` mirrors markup into `#roomWizardLookPreviewVisual` and unhides `#roomWizardLookPreviewStrip` so the local Gemini flow (status “Preview ready — apply or discard.”) shows the environment preview on **1 · Describe** instead of only under **2 · Preview & build**. Bumped cache `?v=202604132` on wizard/editor script tags in `room-layout-editor.html`. Ran `node --test tests/room-wizard-environment-copilot.test.js tests/room-wizard-results-contract.test.js tests/game-logic.test.js` (Pass). AT-01–AT-09 not run; Describe-step preview not visually re-validated in CI.

Update 2026-04-13 (later): **Local vs project Copilot UX** — Without `?project_id=…`, Generate only runs `/api/copilot` (theme/tags + SVG layout diagram), not Gemini image previews. Updated `updateRoomWizardCopilotHintUi` + `#roomWizardLookPreviewExplainer`, generate button label (`Suggest look (diagram)` vs `Generate preview pictures`), and success status copy in `chunk-2.js` / `chunk-3.js`. Cache `?v=202604133`. Ran `node --test tests/room-wizard-environment-copilot.test.js tests/room-wizard-results-contract.test.js tests/game-logic.test.js` (Pass). AT-01–AT-09 not run.
